# RELEASE-2026-04-18-06

## 本次发布日期
- 2026-04-18

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-6`

## 本次发布目的
- 为 Microsoft OAuth callback 增加服务端诊断日志，定位 popup 关闭但账号未写入数据库的问题。
- 强化 popup 回传兜底，避免 popup 关闭失败或消息未送达时原页面完全收不到结果。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `server/app.ts`
- `src/state/admin-console.ts`
- `releases/RELEASE-2026-04-18-06.md`

## 本次更新内容摘要
- 在 [server/app.ts](server/app.ts#L370-L449) 的 Microsoft OAuth callback 各关键阶段增加结构化日志，包括 state 校验、token exchange、Graph `/me`、账号 upsert 与最终成功分支。
- 在 [server/app.ts](server/app.ts#L3972-L4017) 的 popup 回传页中增加延迟关闭与 fallback redirect，确保 postMessage 未生效时仍会跳回原页面展示结果。
- 在 [admin-console.ts](src/state/admin-console.ts#L641-L676) 中保留 OAuth 成功提示，并在列表刷新失败时给出明确 warning。

## 本次是否涉及配置变更
- 是
- 部署时需要将服务器 `.env` 中的 `APP_IMAGE` 更新为固定 tag `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-6`。
- 其余 OAuth 环境变量继续沿用当前线上配置。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，沿用现有库结构。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先推送 `dev`，再推送固定 tag `2026.04.18-6`，等待 GHCR 镜像可用。
- 服务器侧只允许执行文档中的检查命令、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- OAuth callback 结构化日志输出检查
- popup postMessage / fallback redirect 代码路径检查
- 服务器部署后 `/api/health` 健康检查

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-5`。
- 本次不涉及数据库结构变更，无需额外执行数据库回滚。

## 风险说明
- 本次主要增加诊断日志和 popup 兜底，不改变 OAuth provider 配置；真实失败原因仍需用户重新触发一次授权后结合日志确认。
- 若浏览器环境完全禁止 `window.close()` 或 `postMessage`，仍将退回 query 回跳路径。

## 审核状态
- 审核通过

## 部署状态
- 未部署
