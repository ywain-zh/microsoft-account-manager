# RELEASE-2026-05-20-03

## 本次发布日期
- 2026-05-20

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.20-3`

## 本次发布目的
- 按 Cloud Mail 接口文档风格重写 Sub2API 下的接口文档页面，让外部系统更容易照着对接。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `src/views/InterfacesView.vue`
- `releases/RELEASE-2026-05-20-03.md`

## 本次更新内容摘要
- 接口文档页改为“接口说明、接口地址、请求头、请求参数、返回字段、请求示例、返回示例、错误返回”的文档结构。
- 明确展示外部微软邮箱列表接口、外部微软邮箱邮件接口、错误码、上传账号接口。
- 保留上传字段映射配置，但放到文档底部，避免干扰外部读取接口。
- 移除原页面里不够清晰的接口总览和管理端接口堆叠说明。

## 本次是否涉及配置变更
- 否

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 中的标准流程执行。
- 本地验证通过后提交 `dev`，推送 tag `2026.05.20-3` 触发 GHCR 镜像构建。
- GHCR 固定 tag 镜像可用后，服务器侧执行固定 tag 镜像拉取、`.env` 备份、`APP_IMAGE` 更新、`docker compose pull`、`docker compose up -d`、日志与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 线上健康检查。
- 验证接口文档路由 `/services/sub2api/interfaces` 可访问。

## 本次回滚方式
- 按 `DEPLOY_LITE.md` 回滚到上一版已验证 tag：`2026.05.20-2`。
- 本次不涉及数据库迁移，通常无需恢复数据库备份。

## 风险说明
- 本次只改接口文档展示，不改外部接口行为。

## 审核状态
- 审核通过：用户要求参考 `https://doc.skymail.ink/api/api-doc.html` 的样式重写接口文档。

## 部署状态
- 已部署

## 实际部署记录
- 部署服务器：`aliyun-server:/opt/microsoft-account-manager`
- 固定镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.20-3`
- 已同步 release 文档：`/opt/microsoft-account-manager/releases/RELEASE-2026-05-20-03.md`
- 已备份 `.env`：`/opt/microsoft-account-manager/.env.bak.2026.05.20-3`
- `docker compose pull` 与 `docker compose up -d` 执行成功。
- 健康检查返回：`{"ok":true}`
- 容器状态：`microsoft-account-manager` 为 `running healthy`
- 线上验证：接口文档路由 `/services/sub2api/interfaces` 返回 `200`。
