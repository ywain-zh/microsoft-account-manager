# RELEASE-2026-04-21-02

## 本次发布日期
- 2026-04-21

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.21-2`

## 本次发布目的
- 修复 779 验卡有效期字段映射错误，确保卡密历史和工作台能正确显示卡片有效期。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `server/app.ts`
- `releases/RELEASE-2026-04-21-02.md`

## 本次更新内容摘要
- 将 779 验卡结果中的有效期来源从仅识别 `expiry_time` 改为优先识别 `expiry_time`，缺失时回退到真实返回的 `expires_at`。
- 为 779 验卡有效期增加统一时间解析与格式化逻辑，兼容 ISO 时间与常见斜杠/横杠日期格式。
- 修复 `card_valid_until` 无法写入导致卡密历史“卡片有效期”列长期显示 `-` 的问题。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅修正已有 779 有效期字段映射与计算逻辑。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.21-2`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。

## 本次验证范围
- `tsc -p tsconfig.server.json --noEmit`
- `esbuild server/main.ts server/cli/migrate.ts server/cli/import-legacy.ts --bundle --platform=node --format=esm --target=node22 --external:better-sqlite3 --outdir=build/server --outbase=server --entry-names=[dir]/[name]`
- 使用卡密 `9E6EB35F0F16444A` 请求真实 779 验卡接口，确认返回包含 `expires_at`
- 本地确认后端会把 779 验卡有效期规范化并用于计算 `cardValidUntil`

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.21-1`。
- 本次不涉及数据库结构变更，无需额外执行数据迁移回滚。

## 风险说明
- 779 上游若再次变更有效期字段名或返回格式，仍需同步调整映射规则。
- 旧数据只有在重新验卡/提取后才会刷新 `card_valid_until`；历史中已落库的空值不会自动批量回填。

## 审核状态
- 审核通过

## 部署状态
- 未部署
