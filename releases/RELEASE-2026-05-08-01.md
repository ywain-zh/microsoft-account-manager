# RELEASE 2026-05-08-01

## 本次发布日期
- 2026-05-08

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.08-1`

## 本次发布目的
- 发布本轮管理后台界面整理与邮箱列表体验修复，统一全局品牌为“望月工具箱”，并补齐微软邮箱备注能力。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `index.html`
- `public/favicon-mail.svg`
- `public/wangyue-logo.svg`
- `src/components/WangyueLogo.vue`
- `src/config/navigation.ts`
- `src/layouts/AdminShell.vue`
- `src/router.ts`
- `src/state/admin-console.ts`
- `src/style.css`
- `src/views/AccountsView.vue`
- `src/views/CloudMailAccountsView.vue`
- `src/views/LoginView.vue`
- `releases/RELEASE-2026-05-08-01.md`

## 本次更新内容摘要
- 全局名称统一调整为“望月工具箱”，并替换为统一的望月 logo 与 favicon。
- 侧边栏移除 `779验卡` 入口，并将 `Sub2API 检测` 调整到菜单底部。
- Cloud Mail 页面隐藏外层配置摘要信息，仅保留配置弹框查看；移除邮箱列表中的 `GPT` 跳转入口。
- Cloud Mail 与微软邮箱搜索框输入后自动刷新列表搜索结果。
- 微软邮箱列表移除 `Client ID`、`Refresh Token` 与倒计时展示，新增与 Cloud Mail 一致形式的备注列和备注编辑弹框。

## 本次是否涉及配置变更
- 是
- 仅涉及生产 `.env` 中 `APP_IMAGE` 更新为固定镜像 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.08-1`。
- 不新增、不删除环境变量。

## 本次是否涉及数据结构/迁移
- 否
- 备注能力复用现有账户备注字段，不涉及数据库结构迁移。

## 本次部署步骤
- 严格引用 `DEPLOY_LITE.md` 中的标准步骤。
- 发布来源为 `dev`。
- 本地完成类型检查与构建后推送 `dev`，再推送 tag `2026.05.08-1` 触发 GHCR 镜像发布。
- GHCR 固定 tag 镜像可用后，服务器仅执行固定 tag 镜像拉取、`.env` 备份、`APP_IMAGE` 更新、`docker compose pull`、`docker compose up -d`、日志与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 关键页面范围：Cloud Mail、微软邮箱、Sub2API 检测、全局导航与品牌展示。

## 本次回滚方式
- 将服务器 `.env` 中 `APP_IMAGE` 切回上一版已验证镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.06-2`。
- 执行 `docker compose pull` 与 `docker compose up -d`。
- 本次不涉及数据库结构迁移，正常情况下不需要恢复数据库备份。

## 风险说明
- 本次主要风险集中在管理后台前端展示与列表交互，需重点观察 Cloud Mail 和微软邮箱列表搜索、备注保存、导航入口与 logo 展示。
- 若上线后出现页面加载异常或列表交互异常，按回滚方式切回 `2026.05.06-2`。

## 审核状态
- 审核通过
- 用户已明确要求按照部署文档直接部署。

## 部署状态
- 已部署
- 服务器已切换到 `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.08-1`。
- 容器状态：`running healthy`。
- 健康检查：`{"ok":true}`。
