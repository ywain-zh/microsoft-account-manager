# RELEASE-2026-08-01-02

## 本次发布日期
- 2026-08-01

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.08.01-2`

## 本次发布目的
- 修复宝可梦登录 multipart 表单为空的问题，并将宝可梦多账号续费整合到公益站账号列表和添加账号弹框。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否。

## 本次是否涉及冲突处理
- 否。

## 本次修改文件列表
- `server/runtime/pokemon-renewal.ts`
- `src/views/PublicCheckinView.vue`
- `src/views/PokemonRenewalView.vue`
- `src/layouts/AdminShell.vue`
- `src/router.ts`
- `tests/pokemon-renewal.test.ts`
- `releases/RELEASE-2026-08-01-02.md`

## 本次更新内容摘要
- 宝可梦站点请求统一使用 `undici.fetch` 与 `undici.FormData`，避免 multipart 字段被发送为空。
- 优先展示站点结构化字段错误，不记录密码、Token、请求头或原始响应。
- “添加账号”弹框新增公益站/宝可梦类型选择，默认保持公益站原流程。
- 宝可梦在公益站列表中聚合为单条记录，操作列仅保留“续费”。
- 宝可梦弹框内统一维护多个账号、全局优惠码、批量续费、实时日志和运行总结。
- 账号编辑改为弹框内联区域；存在未保存编辑时禁止启动续费。
- 删除最后一个宝可梦账号后聚合行消失，历史运行和优惠码配置保留。
- 旧宝可梦独立路由重定向到公益站签到页面。

## 本次是否涉及配置变更
- 否。不新增或删除 `.env` 配置项。
- 现有宝可梦账号密码与优惠码密文保持兼容，无需重新录入。

## 本次是否涉及数据结构/迁移
- 否。不新增迁移，不修改现有三张宝可梦续费表。

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的后续升级流程执行。
- 发布来源固定为 `dev`，服务器仅拉取固定 tag 镜像并重启容器，不在服务器构建。
- 固定变量：
  - `IMAGE_TAG=2026.08.01-2`
  - `PREV_IMAGE_TAG=2026.08.01-1`
  - `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.08.01-2`
  - `PREV_APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.08.01-1`
  - `RELEASE_DOC=releases/RELEASE-2026-08-01-02.md`

## 本次验证范围
- 宝可梦专项测试：7 项全部通过。
- 全量测试：87 项全部通过。
- `npm run typecheck` 通过。
- `npm run build` 通过，Web 与 Server 生产构建成功。
- 隔离生产实例验证默认公益站类型、切换宝可梦、聚合行、内联账号编辑、旧路由重定向。
- 桌面和 390px 窄屏弹框通过视觉验收，页面无横向溢出，浏览器无控制台错误。

## 本次回滚方式
- 将服务器 `.env` 中 `APP_IMAGE` 切回 `ghcr.io/ywain-zh/microsoft-account-manager:2026.08.01-1`。
- 按 `DEPLOY_LITE.md` 执行 `docker compose pull`、`docker compose up -d`、日志与健康检查。
- 本次没有数据库迁移，回滚不需要恢复数据库；部署前仍按标准流程保留数据库备份。

## 风险说明
- 宝可梦适配仍依赖 `web1.52pokemon.cc` 当前协议、固定套餐 ID `8` 和月付周期 `month_price`。
- UI 聚合行只代表一组宝可梦账号，不参与普通公益站签到、余额、公告和模型检测。
- 续费仍只处理启用账号，只有确认月付金额为零才创建和结算订单。
- 本次部署不自动执行真实续费，部署后由管理员手动运行。

## 审核状态
- 审核通过。
- 用户已明确选择实现并部署。

## 部署状态
- 待部署。

## 部署结果
- 待补充。
