# Release 2026.05.17-1

## 基本信息

- 发布日期：2026-05-17
- 来源分支：`dev`
- 镜像版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.17-1`
- 回滚版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.16-3`

## 发布目的

优化 Sub2API 检测统计：套餐未识别的可用账号计入 Free，并支持点击查看 401 账号明细和报错信息。

## 修改文件

- `server/app.ts`
- `src/state/sub2api-console.ts`
- `src/views/Sub2ApiCheckerView.vue`
- `releases/RELEASE-2026-05-17-01.md`

## 更新内容

- Sub2API 检测成功但套餐未识别时，计入 `Free 可用`。
- `401 账号数` 统计卡片支持点击。
- 新增 `401 账号列表` 弹窗，展示账号和报错信息。
- 401 明细弹窗复用异常账号列表的表格和分页体验。

## 配置变更

- 无新增环境变量。

## 数据结构/迁移

- 无数据库结构变更。
- 无迁移脚本。

## 验证记录

- `npm run typecheck`
- `npm run build`
- `git diff --check`

## 部署步骤

1. 提交本次变更到 `dev`。
2. 推送 `dev`。
3. 创建并推送 tag `2026.05.17-1`。
4. 等待 GitHub Actions 构建并发布 GHCR 固定 tag 镜像。
5. 在阿里云服务器 `/opt/microsoft-account-manager` 备份 `.env` 和 SQLite 数据库。
6. 将 `.env` 中 `APP_IMAGE` 更新为 `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.17-1`。
7. 执行 `docker compose pull` 和 `docker compose up -d`。
8. 检查容器状态、日志、资源占用和健康接口。

## 回滚方式

如需回滚，切回上一版已验证镜像：

```bash
cd /opt/microsoft-account-manager
sed -i "s#^APP_IMAGE=.*#APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.05.16-3#" .env
docker compose pull
docker compose up -d
docker compose exec -T microsoft-account-manager wget -qO- http://127.0.0.1:8787/api/health
```

## 风险说明

- 本次只调整 Sub2API 检测统计和前端明细查看，无数据库迁移。
- 401 明细依赖检测日志中携带的账号 ID 和报错信息。

## 审核状态

- 用户已明确要求部署到阿里云服务器。

## 部署状态

- 待部署
