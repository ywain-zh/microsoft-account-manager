# RELEASE-2026-07-28-01

## 本次发布日期
- 2026-07-28

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.07.28-1`

## 本次发布目的
- 为雨落千岛公益开放平台增加签到与积分余额查询支持。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否。

## 本次是否涉及冲突处理
- 否。

## 本次修改文件列表
- `server/runtime/public-checkin.ts`
- `tests/public-checkin.test.ts`
- `releases/RELEASE-2026-07-28-01.md`

## 本次更新内容摘要
- 精确匹配 `platform.rainflowtb.com` 并选择专用公益站适配器。
- 使用现有加密 Key 保存用户 token，请求时仅通过 `X-User-Token` 发送。
- 通过 `GET /user/api/checkin` 查询 `points.balance`。
- 签到前检查当日状态，可签到时通过 `POST /user/api/checkin` 签到并读取 `record.points`。
- 当日已签到时不重复请求或累计奖励；上游异常返回明确错误。

## 本次是否涉及配置变更
- 否。不新增或删除 `.env` 配置项，不修改现有部署参数。
- 仅将线上固定镜像 tag 更新为 `ghcr.io/ywain-zh/microsoft-account-manager:2026.07.28-1`。

## 本次是否涉及数据结构/迁移
- 否。不新增迁移，不修改现有数据库结构。

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的标准发布与后续升级部署流程执行。
- 发布来源固定为 `dev`。
- 服务器只拉取固定镜像 tag 并重启容器，不在服务器执行构建。
- 固定变量：
  - `IMAGE_TAG=2026.07.28-1`
  - `PREV_IMAGE_TAG=2026.07.14-1`
  - `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.07.28-1`
  - `PREV_APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.07.14-1`
  - `RELEASE_DOC=releases/RELEASE-2026-07-28-01.md`

## 本次验证范围
- `node --test --experimental-strip-types tests/public-checkin.test.ts`：63 项全部通过。
- `npm run typecheck` 通过。
- `npm run build` 通过，Web 与 Server 生产构建均成功。
- 使用用户提供的 HAR 在真实站点验证：`X-User-Token` 鉴权成功，积分余额查询返回 `10.51`；当日已签到时正确跳过重复 POST。
- 本地生产服务 `/login` 返回 HTTP 200。
- 部署后检查容器状态、日志、资源占用、容器内 `/api/health` 和公网健康接口。

## 本次回滚方式
- 将服务器 `.env` 中 `APP_IMAGE` 切回已验证镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.07.14-1`。
- 按 `DEPLOY_LITE.md` 执行 `docker compose pull`、`docker compose up -d`、日志与健康检查。
- 本次无数据库迁移，不需要恢复数据库。

## 风险说明
- 适配基于当前 HAR 中的 `/user/api/checkin` 协议和 `X-User-Token` 鉴权；若上游后续调整字段或鉴权方式，需要同步更新适配器。
- 用户 token 仍按现有凭证机制加密保存；本次发布不会导入 HAR 中的 token。
- 仅精确域名 `platform.rainflowtb.com` 启用该逻辑，不影响其他 New API 站点。

## 审核状态
- 审核通过。
- 用户已明确要求按照部署文档部署到线上阿里云服务器，视为批准本次部署。

## 部署状态
- 已部署。

## 部署结果
- Git 发布提交：`b82a01d32863871d3d96c4aaee4c717a62909274`。
- GitHub tag：`2026.07.28-1`。
- GitHub Actions `Publish GHCR Image`（run `30351271676`）执行成功。
- 服务器侧已确认 `ghcr.io/ywain-zh/microsoft-account-manager:2026.07.28-1` manifest 可见。
- Release 文档已同步到 `/opt/microsoft-account-manager/releases/RELEASE-2026-07-28-01.md`。
- 服务器 `.env` 已更新为 `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.07.28-1`。
- 已创建部署前环境备份 `/opt/microsoft-account-manager/.env.bak.2026.07.28-1`。
- 已执行 `docker compose pull` 和 `docker compose up -d`，服务器未执行构建。
- 容器 `microsoft-account-manager` 当前使用 `2026.07.28-1`，状态为 `running healthy`。
- 容器内 `/api/health` 返回 `{"ok":true}`；公网 `https://mail.0222999.xyz/api/health` 返回 HTTP 200 和 `{"ok":true}`，公网首页返回 HTTP 200。
- 部署后容器内存约 `53.44MiB / 512MiB`，CPU 状态正常。
- 健康检查成功后已删除旧镜像 `2026.06.28-1`，当前仅保留 `2026.07.28-1` 和回滚版本 `2026.07.14-1`。
- 清理前根分区使用率 34%、Docker 镜像占用 6.694GB；清理后根分区使用率 33%、Docker 镜像占用 6.225GB。
- 启动日志存在 Tini 子进程提示及一个既有公益站公告防护挑战；两者均未影响容器健康或本次雨落千岛适配器部署。
