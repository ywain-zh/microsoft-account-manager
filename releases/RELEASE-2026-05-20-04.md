# RELEASE-2026-05-20-04

## 本次发布日期
- 2026-05-20

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.20-4`

## 本次发布目的
- 调整外部微软邮箱列表接口：默认返回全部微软邮箱账号，不再要求必须传邮箱关键词。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/app.ts`
- `src/views/InterfacesView.vue`
- `releases/RELEASE-2026-05-20-04.md`

## 本次更新内容摘要
- `GET /api/external/microsoft/accounts` 的 `email` 参数改为可选。
- 不传 `email` 时返回全部微软邮箱账号。
- 传 `email` 时仍可按邮箱关键词模糊筛选。
- 接口文档同步改为“查询全部微软邮箱列表”，并标注 `email` 为可选参数。

## 本次是否涉及配置变更
- 否

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 中的标准流程执行。
- 本地验证通过后提交 `dev`，推送 tag `2026.05.20-4` 触发 GHCR 镜像构建。
- GHCR 固定 tag 镜像可用后，服务器侧执行固定 tag 镜像拉取、`.env` 备份、`APP_IMAGE` 更新、`docker compose pull`、`docker compose up -d`、日志与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 线上健康检查。
- 线上验证 `GET /api/external/microsoft/accounts` 不传 `email` 可返回全部微软邮箱列表。
- 线上验证接口文档路由 `/services/sub2api/interfaces` 可访问。

## 本次回滚方式
- 按 `DEPLOY_LITE.md` 回滚到上一版已验证 tag：`2026.05.20-3`。
- 本次不涉及数据库迁移，通常无需恢复数据库备份。

## 风险说明
- 该接口不传筛选条件时会返回全部微软邮箱账号，外部调用方需要妥善保管开放接口 Key。

## 审核状态
- 审核通过：用户明确要求列表接口返回全部微软邮箱，而不是必须按邮箱名查询。

## 部署状态
- 待部署
