# RELEASE-2026-04-22-04

## 本次发布日期
- 2026-04-22

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-4`

## 本次发布目的
- 将 779 左侧历史地址里的邮编与国家拆成两个独立字段展示。
- 继续补齐旧历史地址格式兼容，让缺少州字段的数据也能尽量解析。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/views/Seven79CardsView.vue`
- `releases/RELEASE-2026-04-22-02.md`
- `releases/RELEASE-2026-04-22-03.md`
- `releases/RELEASE-2026-04-22-04.md`

## 本次更新内容摘要
- 779 左侧地址详情字段从“邮编 / 国家”合并展示改成“邮编”“国家”两个独立信息块，复制按钮分别独立。
- 历史地址解析补充兼容 `街道, 城市 邮编, 国家` 这种缺少州字段的旧格式，例如 `239 S TEXAS AVE,HENDERSON 89015,US`。
- 对于仍然无法识别的地址，继续回退为完整地址展示，避免把旧数据解析坏。
- 同步把前两次已完成部署的 release 文档状态补记为“已部署”，保证仓库记录与线上实际一致。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅调整 779 前端地址展示与解析逻辑。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.22-4`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 若镜像暂未可用，必须继续等待并自动重试，镜像可用后直接继续部署，不得中途停下。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 确认 779 左侧地址字段已拆分为“街道 / 城市 / 州 / 邮编 / 国家”。
- 确认历史地址 `239 S TEXAS AVE,HENDERSON 89015,US` 能解析出街道、城市、邮编、国家。
- 确认解析失败时仍能保留完整地址展示。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-3`。
- 本次不涉及数据库结构变更，无需额外执行迁移回滚。

## 风险说明
- 历史地址数据来源格式仍可能继续出现未覆盖变体，未命中的情况下仍会回退为完整地址展示。
- 本次主要为前端展示和解析兼容调整，部署后应重点回归 779 历史卡片切换、地址字段复制与右侧接码区展示。

## 审核状态
- 审核通过

## 部署状态
- 未部署
