# Release 2026.05.16-1

## 本次发布日期
- 2026-05-16

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.16-1`

## 本次发布目的
- 在 Cloud Mail 邮箱列表中增加 GPT 有效性手动检测入口。
- 通过 Sub2API 检测对应邮箱的 GPT 账号是否可用，有效时在列表显示绿色状态。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/app.ts`
- `src/api.ts`
- `src/types.ts`
- `src/state/cloud-mail-console.ts`
- `src/views/CloudMailAccountsView.vue`
- `releases/RELEASE-2026-05-16-01.md`

## 本次更新内容摘要
- 新增 Sub2API GPT 有效性检测接口：按邮箱查找 Sub2API GPT 账号，并复用现有模型检测逻辑判断是否可用。
- 前端 API 与类型补齐 GPT 有效性检测响应。
- Cloud Mail 列表在“备注”和“分享”之间新增 `GPT有效` 列。
- 每行默认显示 `检测` 按钮；检测中显示 `检测中`；检测有效后显示绿色 `GPT有效`；检测无效或未找到时不显示无效状态。

## 本次是否涉及配置变更
- 否
- 不新增环境变量，不修改服务器部署参数；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 不新增数据库迁移，不修改现有数据表结构。

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的标准流程执行。
- 发布来源为 `dev`。
- 先完成本地验证、提交并推送 `dev`、推送固定 tag `2026.05.16-1`。
- 等待 GHCR 固定 tag 镜像可用后，服务器侧仅执行固定 tag 镜像拉取、容器更新、日志检查、资源检查和健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 本地浏览器验证 Cloud Mail 表格列顺序：`邮箱 / 备注 / GPT有效 / 分享 / 创建时间 / 操作`

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 切回上一个已验证 tag：`2026.05.15-1`。
- 本次不涉及数据库迁移，通常无需恢复数据库备份；若部署前已做数据库备份，可按标准故障流程恢复。

## 风险说明
- GPT 有效性检测会调用 Sub2API 管理接口和模型检测链路；若 Sub2API 配置不可用或远端检测超时，单行检测会失败但不影响 Cloud Mail 邮箱列表读取。
- 检测无效时列表不显示无效文案，避免误导批量查看；需要再次确认时可刷新页面后重新手动检测。

## 审核状态
- 审核通过：用户已明确要求按照部署文档部署到阿里云服务器，直接部署，不再等待后续同意。

## 部署状态
- 已部署
- 部署时间：2026-05-16
- 服务器镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.16-1`
- 服务器目录：`/opt/microsoft-account-manager`
- 环境备份：`/opt/microsoft-account-manager/.env.bak.2026.05.16-1`
- 数据库备份：`/opt/microsoft-account-manager/data/account-manager.db.bak.2026.05.16-1`
- 健康检查：`{"ok":true}`
- 公网健康检查：`http://47.251.82.144:8787/api/health` 返回 `{"ok":true}`
- 容器状态：`running healthy`
