# RELEASE 2026-05-29-01

## Summary

- Sub2API GPT 检测默认模型从 `gpt-5.4` 更新为 `gpt-5.5`。
- 邮箱列表页 GPT 状态检测、Sub2API 检测页、重新授权后的验证默认模型保持一致。
- 翻译功能默认模型保持 `gpt-5.4-mini` 不变。

## Verification

- `npm run typecheck`
- `npm run build`
