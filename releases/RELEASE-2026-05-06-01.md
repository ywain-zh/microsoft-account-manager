# Release 2026.05.06-1

## 本次发布日期
- 2026-05-06

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.06-1`

## 本次发布目的
- 修复 779 验卡工作台中“提取并验证”偶发返回登录失败 `403` 的问题。
- 避免同一次验卡请求重复调用 779 新接口，降低上游接口短时间拒绝导致前端误报失败的概率。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/app.ts`
- `releases/RELEASE-2026-05-06-01.md`

## 本次更新内容摘要
- `/api/779/cards/check` 现在复用 `ensureSeven79CardRefreshedByKey` 已刷新并入库的卡片结果。
- 返回给前端的 `check` 和 `verify` 字段由已刷新后的 `item` 组装，去掉同一请求内第二次调用 `fetchSeven79CardDetails`。
- 本地调用同一卡密的验卡与提取接口均返回 `200 OK`，未复现截图中的 `403` 登录失败提示。

## 本次是否涉及配置变更
- 否
- 仅部署镜像 tag 更新为 `2026.05.06-1`。

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 中的后续升级部署步骤执行。
- 发布来源必须是 `dev`。
- 服务器只允许拉取固定 tag 镜像并执行 `docker compose pull` / `docker compose up -d`，不得在服务器构建。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 本地接口冒烟：
  - `/api/779/cards/check` 使用卡密 `4859-87ED-GRNBQ6KG-4EA7` 返回 `200 OK`
  - `/api/779/cards/3/extract` 返回 `200 OK`

## 本次回滚方式
- 切回上一版已验证 tag：`2026.04.27-1`。
- 本次不涉及数据库结构变更，通常无需恢复数据库备份。
- 如上线后发现 779 验卡流程异常，按 `DEPLOY_LITE.md` 回滚步骤将 `.env` 中 `APP_IMAGE` 改回 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.27-1`，再执行 `docker compose pull` 和 `docker compose up -d`。

## 风险说明
- 风险点：本次改动影响 779 验卡接口响应组装逻辑。
- 控制方式：仍以已刷新入库的同一份上游结果返回给前端，不改变数据库结构和前端请求方式；上线后重点观察 779 验卡、提取、短信展示流程。

## 审核状态
- 审核通过

## 部署状态
- 已部署
- 部署时间：2026-05-06 19:21:05 CST
- 部署镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.06-1`
- 镜像 revision：`45ffcc73f780300a211fbd8b76a280254c1ae6ec`
- 健康检查：`{"ok":true}`
- 容器状态：`running healthy`
