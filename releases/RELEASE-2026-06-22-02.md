# RELEASE 2026-06-22-02

## 本次发布日期
- 2026-06-22

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.22-2`

## 本次发布目的
- 将管理后台登录会话有效期从 24 小时延长到 30 天，减少频繁重新登录。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/app.ts`

## 本次更新内容摘要
- 将 `SESSION_MAX_AGE_SECONDS` 从 `60 * 60 * 24` 调整为 `60 * 60 * 24 * 30`。
- 新登录生成的 `am_session` cookie 和服务端 session token 过期时间同步延长为 30 天。
- 已有登录态不会自动延长，需要重新登录后才会获得 30 天有效期。

## 本次是否涉及配置变更
- 否。

## 本次是否涉及数据结构/迁移
- 否。

## 本次部署步骤
- 必须严格执行 `DEPLOY_LITE.md` 的后续升级部署步骤。
- 发布来源固定为 `dev`。
- 服务器只允许拉取固定镜像 tag 并重启容器，不允许在服务器构建。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- `git diff --check`

## 本次回滚方式
- 按 `DEPLOY_LITE.md` 回滚步骤切回上一版已验证镜像：
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.22-1`
- 本次不涉及数据库结构变更，回滚不需要恢复数据库结构。

## 风险说明
- 旧登录 cookie 仍按旧过期时间失效；部署后需要用户重新登录一次，新的 30 天有效期才会生效。
- 会话有效期延长会提高登录保持便利性，同时也意味着同一浏览器 cookie 的有效窗口变长；仍可通过退出登录或清理 cookie 立即结束会话。
- 本次只修改登录会话时长，不改管理员账号、密码、`SESSION_SECRET` 或其他环境变量。

## 审核状态
- 用户已明确要求“按照部署文档部署下”，视为审核通过并批准部署。

## 部署状态
- 已部署

## 部署结果
- GitHub Actions `Publish GHCR Image` 已成功构建并推送镜像，运行 ID：`27957743491`。
- 服务器已同步 release 文档到 `/opt/microsoft-account-manager/releases/RELEASE-2026-06-22-02.md`。
- 服务器侧 `docker manifest inspect ghcr.io/ywain-zh/microsoft-account-manager:2026.06.22-2` 校验通过。
- 服务器 `/opt/microsoft-account-manager/.env` 已更新为 `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.06.22-2`。
- 已创建部署前备份：
- `.env.bak.2026.06.22-2`
- `data/backups/account-manager.2026.06.22-2.20260622215728.db`
- 已执行 `docker compose pull` 和 `docker compose up -d`，未在服务器构建。
- 当前容器 `microsoft-account-manager` 使用镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.22-2`，状态为 `running healthy`。
- 容器内健康检查 `http://127.0.0.1:8787/api/health` 返回 `{"ok":true}`。
- 线上公网健康检查 `https://mail.0222999.xyz/api/health` 返回 HTTP 200 和 `{"ok":true}`。
- 线上首页 `https://mail.0222999.xyz/` 返回 HTTP 200。
- 容器资源占用正常，部署后内存约 `62.88MiB / 512MiB`。
- 已清理旧项目镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-5`。
- 当前保留镜像：
- 当前版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.22-2`
- 回滚版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.22-1`
- 清理前根分区约 `30%` 使用率，清理后约 `28%` 使用率。
- 未执行回滚。
