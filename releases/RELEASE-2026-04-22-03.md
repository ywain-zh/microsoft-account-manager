# RELEASE-2026-04-22-03

## 本次发布日期
- 2026-04-22

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-3`

## 本次发布目的
- 将 779 左侧虚拟卡信息区进一步调整为图二那种逐行展示样式。
- 放宽历史地址解析兼容范围，让旧地址数据也能尽量拆分显示。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/views/Seven79CardsView.vue`
- `releases/RELEASE-2026-04-22-03.md`

## 本次更新内容摘要
- 779 左侧虚拟卡信息区改为图二那种逐行块状展示：卡号独立一行、日期与 CVV 并排、地址区域按行向下堆叠展示。
- 左侧复制交互改成图二样式的文字按钮“复制”，不再使用我上一版那种小图标块。
- 地址字段文案改为“街道 / 城市 / 州 / 邮编 / 国家”这一组中文展示，其中邮编与国家合并展示。
- 放宽历史地址解析，兼容更多旧格式：带逗号、多段逗号、无国家、`USA` / `United States` 等变体。
- 当历史地址仍无法识别时，继续回退为完整地址展示，避免把旧数据解析坏。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅调整 779 前端展示与地址解析逻辑。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.22-3`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 若镜像暂未可用，必须继续等待并自动重试，镜像可用后直接继续部署，不得中途停下。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 确认 779 左侧虚拟卡信息区已按图二改成逐行展示。
- 确认历史地址格式能尽量拆分到“街道 / 城市 / 州 / 邮编 / 国家”。
- 确认解析失败时仍能保留完整地址展示。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-2`。
- 本次不涉及数据库结构变更，无需额外执行迁移回滚。

## 风险说明
- 历史地址数据来源格式可能继续出现未覆盖变体，未命中的情况下仍会回退为完整地址展示。
- 本次为前端样式与解析规则调整，部署后应重点回归 779 左侧虚拟卡显示、复制按钮和历史卡片切换场景。

## 审核状态
- 审核通过

## 部署状态
- 未部署
