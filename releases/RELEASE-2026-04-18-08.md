# RELEASE-2026-04-18-08

## 本次发布日期
- 2026-04-18

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-8`

## 本次发布目的
- 修复 Microsoft OAuth popup 授权成功后前端列表未稳定刷出新绑定账号的问题。
- 在授权成功后优先定位新绑定账号，并在搜索结果未命中时自动回退到全量列表，避免用户误以为绑定失败。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/state/admin-console.ts`
- `src/views/AccountsView.vue`
- `releases/RELEASE-2026-04-18-08.md`

## 本次更新内容摘要
- 将 [admin-console.ts](src/state/admin-console.ts#L227-L241) 的列表加载结果显式改为布尔返回值，避免 OAuth 成功后的刷新失败被静默吞掉。
- 调整 [admin-console.ts](src/state/admin-console.ts#L665-L691) 的 OAuth 成功处理：先按授权邮箱搜索刷新，如未命中则自动回退全量列表再次刷新。
- 在 [AccountsView.vue](src/views/AccountsView.vue#L377-L389) 中增加搜索词变化时的分页重置，避免新账号已加载但仍停留在旧分页造成用户看不到结果。

## 本次是否涉及配置变更
- 否
- 本次无需新增或修改服务器环境变量。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，沿用现有库结构。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先推送 `dev`，再推送固定 tag `2026.04.18-8`，等待 GHCR 镜像可用。
- 服务器侧只允许执行文档中的检查命令、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- OAuth popup 成功回传后列表刷新逻辑检查
- 搜索词切换后的分页重置检查
- 服务器部署后 `/api/health` 健康检查

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-6`。
- 本次不涉及数据库结构变更，无需额外执行数据库回滚。

## 风险说明
- 本次仅修正前端刷新与展示逻辑，不改变后端 OAuth 成功判定；若后续再次出现账号未显示，需要优先区分为“未落库”还是“已落库但前端筛选态未命中”。
- 新逻辑在授权邮箱未命中当前筛选结果时会自动回退到全量列表，用户原有搜索词会被清空。

## 审核状态
- 审核通过

## 部署状态
- 已部署
