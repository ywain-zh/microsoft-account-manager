# RELEASE-2026-04-18-04

## 本次发布日期
- 2026-04-18

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-4`

## 本次发布目的
- 将 Microsoft OAuth 回调地址从旧域名切换到正式域名 `mail.0222999.xyz`。
- 保持现有 OAuth 登录与 callback 处理逻辑不变，仅修正配置与部署目标，确保线上授权回跳地址一致。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `.env.example`
- `releases/RELEASE-2026-04-18-04.md`

## 本次更新内容摘要
- 将 `.env.example` 中的 `APP_IMAGE` 更新为固定 tag `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-4`。
- 将 `.env.example` 中的 `MS_REDIRECT_URI` 更新为 `https://mail.0222999.xyz/auth/microsoft/callback`。
- 新增本次 release 文档，记录正式回调域名切换、部署步骤、回滚方式与风险说明。

## 本次是否涉及配置变更
- 是
- `MS_REDIRECT_URI` 需要更新为 `https://mail.0222999.xyz/auth/microsoft/callback`。
- 部署时需要将服务器 `.env` 中的 `APP_IMAGE` 更新为固定 tag `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-4`。
- 如 Azure App Registration 仍使用旧回调地址，需要同步改为新域名回调地址。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，沿用已执行的 `migrations/0005_accounts_auth_type.sql` 结果。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先推送 `dev`，再推送固定 tag `2026.04.18-4`，等待 GHCR 镜像可用。
- 服务器侧只允许执行文档中的检查命令、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 部署时同步更新服务器 `.env`：将 `MS_REDIRECT_URI` 切换为新正式回调地址。

## 本次验证范围
- 配置文件 diff 检查
- release 文档完整性检查
- tag 流程检查
- 服务器部署后 `/api/health` 健康检查

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-3`。
- 同时将服务器 `.env` 中的 `MS_REDIRECT_URI` 恢复为上一个可用回调地址。
- 本次不涉及数据库结构变更，无需额外执行数据库回滚。

## 风险说明
- 若 Azure App Registration 未同步登记 `https://mail.0222999.xyz/auth/microsoft/callback`，微软授权完成后会在提供商侧报回调不匹配。
- 本次主要变更为配置切换，部署后需要重点观察 OAuth 发起跳转与 callback 回站是否正常。
- 真实 OAuth 成功链路仍需使用真实微软账号做一次线上完整冒烟。

## 审核状态
- 审核通过

## 部署状态
- 未部署
