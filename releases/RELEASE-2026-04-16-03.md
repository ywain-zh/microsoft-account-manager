# RELEASE-2026-04-16-03

## 本次发布日期
- 2026-04-16

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.16-3`

## 本次发布目的
- 按 `Gemini设计搞/收件箱/收件箱.html` 与 `Gemini设计搞/收件箱/UI_DESIGN_SPEC.md` 继续收敛收件箱弹窗 UI。
- 在不修改业务逻辑、不改接口调用和按钮功能的前提下，把收件箱弹窗的头部、左右分栏、邮件列表卡片和正文阅读区调整到更接近设计稿的形态。

## 本次修改文件列表
- `src/components/MailInboxViewer.vue`
- `src/style.css`
- `releases/RELEASE-2026-04-16-03.md`

## 本次更新内容摘要
- 收件箱弹窗头部改为与设计稿一致的“标题 + 当前邮箱 + 复制/刷新微操作按钮 + 关闭按钮”布局。
- 左侧邮件列表调整为接近设计稿的 `320px` 固定侧栏与白色卡片式邮件项，统一选中态、hover 态和文本层级。
- 右侧正文区统一为白底阅读区，重整主题、发件人/收件人/时间信息和正文 iframe 容器的留白与分隔。
- 弹窗容器统一为 `12px` 圆角和 `0 20px 60px rgba(0, 0, 0, 0.15)` 阴影，并补齐小型操作按钮、滚动条和响应式样式。
- 保持现有 `script setup`、props、emit、邮件选择、复制、刷新、关闭和正文渲染逻辑不变。

## 本次是否涉及配置变更
- 否
- 本次不新增环境变量，不修改部署参数，不调整服务端配置。

## 本次是否涉及数据结构/迁移
- 否
- 本次仅涉及前端模板与样式调整，不涉及数据库结构、迁移脚本和数据导入流程。

## 本次部署步骤
- 严格执行 `DEPLOY_LITE.md` 中的标准流程：本地验证 -> release 文档 -> 推送代码 -> push tag 发镜像 -> 确认 GHCR tag 镜像可用 -> 服务器 pull/up -> 健康检查。
- 服务器侧只允许执行文档中的检查命令、`docker compose pull`、`docker compose up -d`、日志检查、资源检查和健康检查命令。
- 禁止在服务器执行 `docker build`、`docker compose up -d --build`、`npm install`、`npm run build`、`vite build`。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.16-2`。
- 本次发布不涉及数据库迁移，如仅为 UI 回退，无需恢复数据库备份。

## 风险说明
- 本次改动集中在收件箱弹窗模板和全局样式覆盖，重点观察头部按钮排布、左侧列表宽度、选中态描边、正文区留白和窄屏堆叠。
- `src/style.css` 中存在集中式弹窗样式，本次新增了收件箱 HTML 对齐覆盖层，需要重点观察与已有全局样式的优先级关系。
- 部署前必须确认 GHCR 中已生成固定 tag `2026.04.16-3` 的镜像，否则立即停止，不允许进入服务器升级步骤。

## 审核状态
- 审核通过

## 部署批准状态
- 已批准

## 部署环境
- dev

## 部署状态
- 已部署

## 部署记录
- 部署时间：2026-04-16 18:10 ~ 18:14（Asia/Shanghai）
- 发布分支：`dev`
- 发布提交：`f4f2e1e985b8f30ef2a964dc757859b4cdfd362f`
- 发布 tag：`2026.04.16-3`
- 镜像构建：GitHub Actions `Publish GHCR Image` 成功，workflow run `24504506841`
- 部署目录：`/opt/microsoft-account-manager`
- 当前镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.16-3`
- `.env` 备份：`/opt/microsoft-account-manager/.env.bak.2026.04.16-3`

## 部署执行结果
- 服务器部署前检查通过：`.env`、`data/`、`releases/RELEASE-2026-04-16-03.md` 均存在，`APP_IMAGE` 未使用 `latest`
- 已按文档执行 `.env` 备份、`docker compose pull` 与 `docker compose up -d`
- `docker compose ps` 结果：容器 `microsoft-account-manager` 正在运行，状态为 `Up ... (healthy)`
- `docker inspect` 结果：`running healthy`
- 容器健康检查：`docker compose exec -T microsoft-account-manager wget -qO- http://127.0.0.1:8787/api/health`
- 健康检查返回：`{"ok":true}`
- 服务公开健康接口 `http://47.251.82.144:8787/api/health` 返回：`{"ok":true}`
- 运行日志中仅看到 Tini subreaper 警告，未发现启动失败、崩溃或接口错误
- 本次无需回滚

## 线上验证结果
- 验证地址：`http://47.251.82.144:8787`
- 登录页可正常打开，管理员账号可以成功登录
- 微软邮箱列表页 `/services/microsoft-mail/accounts` 可正常渲染，工具栏、表格、分页与真实数据展示正常
- Cloud Mail 页面 `/services/cloud-mail/accounts` 可正常渲染，工具栏、表格与真实数据展示正常
- 接口管理页 `/services/microsoft-mail/interfaces` 可正常渲染，说明区、映射输入框与保存按钮显示正常
- 收件箱弹窗在正式环境可正常打开，头部按钮、左侧邮件列表、右侧正文 iframe 和关闭操作均正常
- 正式环境截图已保存：`C:\Users\zhouyuan\.agent-browser\tmp\screenshots\screenshot-1776334442268.png`
