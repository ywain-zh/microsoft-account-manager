# RELEASE 2026-05-09-01

## 本次发布日期
- 2026-05-09

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.09-1`

## 本次发布目的
- 发布后台 UI-only 统一视觉重构。
- 在不改变功能和按钮行为的前提下，统一颜色、样式、卡片、按钮、表单、表格、弹窗和页面视觉层。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `src/gradient-system.css`
- `src/layouts/AdminShell.vue`
- `releases/RELEASE-2026-05-09-01.md`

## 本次更新内容摘要
- 使用 `ui-ux-pro-max` 推荐的专业浅色后台设计方向，统一为海军蓝主色、蓝色辅助色、绿色强调色和高对比浅色表面。
- 在 `src/gradient-system.css` 追加集中式主题覆盖层，统一 shell、共享面板、弹窗、按钮、表单、表格、标签、登录页、邮箱页、Sub2API、系统设置、779 工作台和收件箱视觉。
- 保留现有 API、状态、事件绑定、表单提交、OAuth、Sub2API 流式检测、复制和表格操作逻辑不变。
- 为当前导航项补充 `aria-current="page"`，提升可访问性，不改变路由行为。

## 本次是否涉及配置变更
- 是
- 生产 `.env` 中 `APP_IMAGE` 需要更新为固定镜像 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.09-1`。
- 不新增环境变量。

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格引用 `DEPLOY_LITE.md` 中的标准步骤。
- 发布来源为 `dev`。
- 本地完成类型检查与构建后推送 `dev`，再推送 tag `2026.05.09-1` 触发 GHCR 镜像发布。
- GHCR 固定 tag 镜像可用后，服务器仅执行固定 tag 镜像拉取、`.env` 备份、`APP_IMAGE` 更新、`docker compose pull`、`docker compose up -d`、日志与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build:web`
- `npm run build`
- Vite 本地服务路由可达性检查：`/login`、Cloud Mail、微软邮箱、Sub2API、系统设置、779 工作台。

## 本次回滚方式
- 将服务器 `.env` 中 `APP_IMAGE` 切回上一版已验证镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.08-4`。
- 执行 `docker compose pull` 与 `docker compose up -d`。
- 本次不涉及数据库迁移；一般无需恢复 SQLite 备份。

## 风险说明
- 主要风险集中在前端样式覆盖范围，可能影响个别页面的视觉间距、颜色或响应式表现。
- 本次不涉及后端、数据库迁移或业务逻辑；如上线后出现严重布局异常，按回滚方式切回 `2026.05.08-4`。

## 审核状态
- 审核通过
- 用户已明确要求按照部署文档部署到阿里云服务器。

## 部署状态
- 未部署
