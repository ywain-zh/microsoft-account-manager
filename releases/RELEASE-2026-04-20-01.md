# RELEASE-2026-04-20-01

## 本次发布日期
- 2026-04-20

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.20-1`

## 本次发布目的
- 上线 779 验卡与接码工作台，补齐卡密导入、验卡提取、PP 接码、历史回填与订阅链接入口。
- 把 779 页面样式继续向当前设计稿收敛，并补齐卡片有效期展示与快捷复制交互。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `server/app.ts`
- `src/api.ts`
- `src/config/navigation.ts`
- `src/layouts/AdminShell.vue`
- `src/router.ts`
- `src/style.css`
- `src/types.ts`
- `src/views/Seven79CardsView.vue`
- `migrations/0006_779_cards.sql`
- `migrations/0007_pp_sms_items.sql`
- `migrations/0008_779_card_valid_until.sql`
- `releases/RELEASE-2026-04-20-01.md`

## 本次更新内容摘要
- 新增 779 卡密与 PP 接码相关数据库结构、后端接口、前端类型与页面路由。
- 新增 779 工作台页面，支持卡密导入、验卡提取、历史切换、验证码获取、复制整卡、PP 接码查看与订阅链接跳转。
- 验卡结果改为落库并回填工作台，历史页支持展示卡片有效期，并按 6 小时有效期显示状态。
- 页面视觉继续向设计稿收敛，统一按钮、输入框、卡片比例、验证码区字体层级与留白。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时只需要把 `APP_IMAGE` 切换到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 是
- 新增 `seven79_cards`、`pp_sms_items` 表，并为 779 卡增加 `card_valid_until` 字段。
- 风险在于首次上线 779 功能会对现有 SQLite 库增加新表和新列；回滚时如需彻底恢复到发布前状态，应先恢复部署前备份的数据库文件，再切回旧镜像。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.20-1`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。

## 本次验证范围
- `vue-tsc --noEmit`
- `tsc -p tsconfig.server.json --noEmit`
- `vite build`
- `esbuild server/main.ts server/cli/migrate.ts server/cli/import-legacy.ts --bundle --platform=node --format=esm --target=node22 --external:better-sqlite3 --outdir=build/server --outbase=server --entry-names=[dir]/[name]`
- 本地 dev 页面冒烟：779 工作台按钮、订阅链接、新窗口跳转

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.19-1`。
- 若本次上线后已执行新迁移且需要回退数据状态，应先恢复部署前备份的 SQLite 数据文件，再拉起旧镜像。

## 风险说明
- 本次是 779 功能首次整体上线，重点观察卡密提取、PP 接码刷新、历史记录切换和卡片有效期显示是否符合预期。
- 本次包含数据库结构变更，需重点确认容器启动后迁移执行正常，以及健康检查和工作台主流程可用。

## 审核状态
- 审核通过

## 部署状态
- 未部署
