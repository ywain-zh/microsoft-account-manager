# RELEASE-2026-04-21-04

## 本次发布日期
- 2026-04-21

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.21-4`

## 本次发布目的
- 修复 779 工作台里 PP 接码有效期长期不显示的问题。
- 支持在 779 工作台主卡片中直接下拉切换 PP 接码手机号。
- 补齐 779 session 获取按钮与前序未部署的有效期显示修正。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `server/app.ts`
- `src/views/Seven79CardsView.vue`
- `releases/RELEASE-2026-04-21-04.md`

## 本次更新内容摘要
- 779 页面新增“获取session”按钮，浏览器可直接尝试读取 `https://chatgpt.com/api/auth/session` 并复制结果。
- 去掉 779 左侧虚拟卡头部额外新增的卡密展示，恢复原先结构。
- 前后端统一修正 779 卡片有效期时间解析，避免 `expires_at` / `YYYY-MM-DD HH:mm:ss` 场景下误判有效期。
- 放宽 PP 接码到期时间提取规则，兼容更宽的标签与日期时间格式，避免主卡片“有效期”显示为 `-`。
- 启用 779 工作台中已存在但被隐藏的 PP 手机号下拉，可在主卡片手动切换接码手机号。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅修正现有前后端展示与解析逻辑。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.21-4`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 确认 PP 主卡片已出现手机号下拉选择器
- 确认 PP 到期时间可由后端提取并返回 `expiresAt`
- 确认“获取session”按钮与原有 779 主流程构建无回归

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.21-2`。
- 本次不涉及数据库结构变更，无需额外执行数据迁移回滚。

## 风险说明
- PP 接码有效期仍依赖上游返回文本；若上游再次改变字段标签或日期语义，仍需继续调整解析规则。
- ChatGPT session 读取仍受浏览器跨域、Cookie 与登录态限制，失败时会提示无法读取，不保证所有浏览器环境都能成功。

## 审核状态
- 审核通过

## 部署状态
- 已部署
