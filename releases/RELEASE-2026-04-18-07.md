# RELEASE-2026-04-18-07

## 本次发布日期
- 2026-04-18

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-6`

## 本次发布目的
- 修正线上 Microsoft OAuth 的 `MS_CLIENT_SECRET` 配置错误，解决 token exchange 阶段 `AADSTS7000215: Invalid client secret provided` 的问题。
- 本次不变更代码，仅修正服务器环境变量并重启当前已部署镜像。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `releases/RELEASE-2026-04-18-07.md`
- 服务器 `.env`（仅线上环境变量变更）

## 本次更新内容摘要
- 将服务器 `.env` 中的 `MS_CLIENT_SECRET` 替换为 Azure App Registration 中客户端密码的正确 Value。
- 保持当前运行镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-6` 不变，按标准流程重新 `docker compose up -d` 使新环境变量生效。

## 本次是否涉及配置变更
- 是
- 仅服务器 `.env` 中 `MS_CLIENT_SECRET` 发生变更。
- `APP_IMAGE` 继续保持 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-6`。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，沿用现有库结构。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 服务器侧只执行检查命令、备份 `.env`、更新 `MS_CLIENT_SECRET`、`docker compose pull`、`docker compose up -d`、日志检查与健康检查。

## 本次验证范围
- 服务器 `.env` 已更新
- `docker compose up -d` 重启成功
- `/api/health` 健康检查
- Microsoft OAuth 日志从 `AADSTS7000215` 继续复测

## 本次回滚方式
- 如需回滚，恢复部署目录下本次生成的 `.env` 备份文件，并重新执行 `docker compose up -d`。
- 本次不涉及数据库结构变更，无需额外执行数据库回滚。

## 风险说明
- 由于用户已在对话中暴露完整 client secret，建议后续在 Azure 中轮换该 secret。
- 若 Azure 侧 secret 已失效、复制不完整或应用权限侧另有问题，仍可能在 token exchange 或 Graph `/me` 阶段失败，需要继续结合新日志排查。

## 审核状态
- 审核通过

## 部署状态
- 已部署
