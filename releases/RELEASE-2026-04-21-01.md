# RELEASE-2026-04-21-01

## 本次发布日期
- 2026-04-21

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.21-1`

## 本次发布目的
- 修复 PP 接码对 HTML 邮件正文的验证码提取，避免误抓旧邮件或无关数字。
- 补齐 779 工作台第一步验卡结果中的卡密有效期显示，并在左侧虚拟卡标题区展示当前卡密。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `server/app.ts`
- `src/views/Seven79CardsView.vue`
- `releases/RELEASE-2026-04-21-01.md`

## 本次更新内容摘要
- PP 接码读取改为自动选择更合适的文本编码，并对 HTML/实体文本做清洗后再提取验证码。
- PP 接码验证码提取改为按上下文给候选数字打分，优先使用 `Your ChatGPT code is`、`verification code`、`验证码` 等附近的数字，降低误命中旧邮件验证码、年份和无关数字的概率。
- 779 第一段快速验卡结果新增“卡密有效期”显示项。
- 779 左侧虚拟卡标题区新增当前卡密展示，便于在工作台直接核对当前选中卡密。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅调整验证码解析与前端展示逻辑。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.21-1`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。

## 本次验证范围
- `vue-tsc --noEmit`
- `tsc -p tsconfig.server.json --noEmit`
- `vite build`
- `esbuild server/main.ts server/cli/migrate.ts server/cli/import-legacy.ts --bundle --platform=node --format=esm --target=node22 --external:better-sqlite3 --outdir=build/server --outbase=server --entry-names=[dir]/[name]`
- 使用 OpenAI HTML 邮件样式样例确认 PP 接码优先提取正文验证码而不是旧邮件列表中的数字
- 本地确认 779 快速验卡结果显示“卡密有效期”
- 本地确认 779 左侧虚拟卡标题区显示当前卡密

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.20-2`。
- 本次不涉及数据库结构变更，无需额外执行数据迁移回滚。

## 风险说明
- PP 接码解析仍依赖邮件正文中的上下文关键词；若上游短信邮件模板大幅变化，仍需重新调整候选评分规则。
- 779 标题区卡密展示长度有限，长卡密会在 UI 中省略显示，需通过悬停查看完整值。

## 审核状态
- 审核通过

## 部署状态
- 未部署
