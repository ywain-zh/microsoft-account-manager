# Release 2026.05.25-2

## 本次发布日期
- 2026-05-25

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.25-2`

## 本次发布目的
- 收敛长链生成器页面 UI，使页面和代理配置弹框与当前后台视觉一致。
- 修复代理配置交互：保存后输入框清空、节点列表保留、测试全部真实回显 IP/地区、测试按钮不关闭弹框。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `src/router.ts`
- `src/views/Sub2ApiLongLinkGeneratorView.vue`
- `releases/RELEASE-2026-05-25-02.md`

## 本次更新内容摘要
- 长链生成器页面改为顶部代理状态卡片和下方生成表单卡片。
- 代理配置按钮移动到代理状态卡片右侧。
- 代理配置弹框改为代理池节点输入、顶部操作按钮和白底代理节点卡片列表。
- 代理节点展示 host:port、地区、协议、脱敏账号和真实测试结果。
- `测试全部` 会合并输入框内未导入代理并逐条真实检测出口 IP。
- 保存代理池后清空输入框，保留下方代理节点列表。

## 本次是否涉及配置变更
- 是
- 生产环境镜像 tag 更新为 `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.25-2`。
- 不新增环境变量，不要求修改线上 `.env` 除 `APP_IMAGE` 外的配置。

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格引用 `DEPLOY_LITE.md` 中的标准轻量部署步骤。
- 发布来源为 `dev`。
- 服务器只执行固定 tag 镜像 pull/up、状态检查、日志检查、资源检查和健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 本地 Chrome DevTools MCP 验证长链生成器页面和代理配置弹框。
- 本地真实代理测试：10 条辣椒代理均返回真实日本出口 IP/地区。
- 本地保存代理池验证：textarea 清空，下方列表保留。

## 本次回滚方式
- 切回上一版已验证 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.25-1`。
- 本次无数据库迁移，正常情况下不需要恢复数据库备份。
- 如部署后健康检查失败，按 `DEPLOY_LITE.md` 回滚步骤修改 `.env` 的 `APP_IMAGE` 并重新 `docker compose pull && docker compose up -d`。

## 风险说明
- 本次主要为前端 UI 和代理配置交互调整，风险集中在页面布局兼容性和代理测试按钮状态。
- 代理真实检测仍依赖代理服务和外部 IP 查询服务，单个代理出口不稳定时可能出现检测失败。

## 审核状态
- 审核通过（用户已明确要求修改并部署到线上）

## 部署状态
- 未部署
