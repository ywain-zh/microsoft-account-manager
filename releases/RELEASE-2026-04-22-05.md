# RELEASE-2026-04-22-05

## 本次发布日期
- 2026-04-22

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-5`

## 本次发布目的
- 将 779 左侧虚拟卡信息统一改成单行展示。
- 调整左侧卡信息与右侧号码区的布局和样式对齐，恢复更自然的视觉节奏。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/views/Seven79CardsView.vue`
- `releases/RELEASE-2026-04-22-05.md`

## 本次更新内容摘要
- 779 左侧卡信息改为单行“标签: 值”展示，卡号支持按 4 位一组格式化显示，例如 `4859 5401 3643 4247`。
- 日期、CVV、持卡人、地址信息及拆分后的地址字段统一改为同一套单行布局，复制按钮继续保留在每行右侧。
- 重新调整左侧信息卡与右侧号码卡的边框、内边距、字号和对齐方式，使左右两栏的视觉样式更加一致。
- 右侧接码区保留上下两个独立号码块，不再误变成一块，同时避免高度被拉得过大。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅调整 779 前端展示样式。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.22-5`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 若镜像暂未可用，必须继续等待并自动重试，镜像可用后直接继续部署，不得中途停下。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 确认 779 左侧卡信息均为单行展示。
- 确认卡号按 4 位一组展示，但复制仍使用原始卡号值。
- 确认右侧两个号码块为上下独立卡片，且整体视觉对齐正常。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-4`。
- 本次不涉及数据库结构变更，无需额外执行迁移回滚。

## 风险说明
- 本次主要为前端展示样式调整，部署后应重点回归 779 左侧卡信息可读性、复制按钮位置以及右侧号码区排版。
- 若极端长地址或异常手机号内容超出常规长度，仍需关注窄宽度下的换行表现。

## 审核状态
- 审核通过

## 部署状态
- 未部署
