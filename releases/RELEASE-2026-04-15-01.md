# RELEASE-2026-04-15-01

## 本次发布日期
- 2026-04-15

## 本次发布版本/tag
- `ghcr.io/your-org/microsoft-account-manager:2026.04.15-1`

## 本次发布目的
- 将 dev 分支的轻量化改造方案、审核门槛和部署流程进一步标准化。
- 把 `DEPLOY_LITE.md` 固化为后续唯一标准部署文档。
- 移除生产示例中对 `latest` 的依赖，并为回滚保留固定 tag 基线。
- 继续收缩运行镜像依赖边界，避免最终镜像继续携带前端运行时依赖。

## 本次修改文件列表
- `REFactor_PLAN.md`
- `DEPLOY_LITE.md`
- `README.md`
- `.env.example`
- `Dockerfile`
- `package.runtime.json`
- `package.runtime-lock.json`
- `releases/RELEASE_TEMPLATE.md`
- `releases/RELEASE-2026-04-15-01.md`

## 本次更新内容摘要
- 将 `REFactor_PLAN.md` 改成准确反映当前状态的版本，并补强“先审核，再部署”的硬约束。
- 重写 `DEPLOY_LITE.md`，补齐部署目标、2C2G 适配说明、部署前检查、首次部署、升级、回滚、日志、健康检查、故障排查和标准命令清单。
- 将 `README.md` 收敛为总览文档，不再作为直接部署执行依据。
- 将 `.env.example` 的 `APP_IMAGE` 改成固定 tag 示例，并明确禁止生产使用 `latest`。
- 新增运行镜像专用 manifest 和 lock 文件，配合 `Dockerfile` 进一步缩小运行镜像依赖边界。

## 本次是否涉及配置变更
- 是
- `APP_IMAGE` 示例 tag 已更新为固定 tag。
- 部署流程约束已增强：部署前必须先编写 release 文档，并且必须先审核、再批准、后部署。

## 本次是否涉及数据结构/迁移
- 否
- 本次不涉及新的数据结构变更或数据库迁移。

## 本次部署步骤
- 部署时必须严格执行 `DEPLOY_LITE.md` 中的标准步骤。
- 没有用户明确审核通过和部署批准，不允许执行部署。

## 本次回滚方式
- 将 `.env` 中的 `APP_IMAGE` 切回上一个已验证 tag，例如 `ghcr.io/your-org/microsoft-account-manager:2026.04.14-2`。
- 按 `DEPLOY_LITE.md` 中的回滚步骤执行 `docker compose pull` 与 `docker compose up -d`。
- 如遇数据库兼容问题，恢复升级前备份的 SQLite 文件。

## 风险说明
- 本次主要是文档和镜像边界标准化，运行逻辑不做业务层改动。
- 需要在后续真正部署前确认镜像 tag 与 release 文档保持一致。
- 需要在本地或 CI 验证新的 Docker 构建过程，确认最终运行镜像已不再包含前端运行时依赖。

## 审核状态
- 待审核

## 部署状态
- 未部署
