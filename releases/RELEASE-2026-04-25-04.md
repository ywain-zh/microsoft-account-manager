# RELEASE 2026-04-25-04

## 本次发布日期
- 2026-04-25

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-4`

## 本次发布目的
- 修复 Cloud Mail 收件箱弹窗“复制”邮箱时偶发复制成旧页面 URL 的问题。

## 本次来源分支
- `dev`
- 本次发布直接从 `dev` 发出，不涉及临时发布分支。

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `src/utils/clipboard.ts`
- `releases/RELEASE-2026-04-25-04.md`

## 本次更新内容摘要
- 公共复制工具改为优先使用 `navigator.clipboard.writeText` 直接写入目标文本。
- 老式选区复制仅作为 Clipboard API 不可用或被阻止时的 fallback，避免浏览器焦点或旧选区导致复制到地址栏 URL。

## 本次是否涉及配置变更
- 否
- 仅需部署固定镜像 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-4`。

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的后续升级部署步骤执行。
- 发布来源必须是 `dev`。
- 服务器只执行固定 tag 镜像 pull、compose up、状态检查、日志检查、资源检查和健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 构建后检查固定 tag 镜像是否已发布到 GHCR。
- 部署后检查容器状态、日志、资源占用和 `/api/health`。

## 本次回滚方式
- 如部署后异常，将 `APP_IMAGE` 切回上一版已验证固定 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-3` 后执行 `docker compose pull` 和 `docker compose up -d`。
- 本次不涉及数据库迁移，不需要恢复数据库备份。

## 风险说明
- 本次修改影响所有调用公共复制工具的入口，应重点检查邮箱复制和其他复制按钮仍可正常提示成功。
- Clipboard API 在极少数非安全上下文或浏览器权限限制下可能失败，此时仍保留原 fallback。

## 审核状态
- 用户已明确要求部署本次复制修复。

## 部署状态
- 未部署
