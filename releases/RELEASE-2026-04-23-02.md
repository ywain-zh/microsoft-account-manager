# RELEASE-2026-04-23-02

## 本次发布日期
- 2026-04-23

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.23-2`

## 本次发布目的
- 调整 Cloud Mail 账号列表操作列，将原“复制”入口改为直接打开 ChatGPT。
- 收紧云邮账号页表格与工具栏间距，减少横向拥挤，提升同屏信息密度。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/views/CloudMailAccountsView.vue`
- `releases/RELEASE-2026-04-23-01.md`
- `releases/RELEASE-2026-04-23-02.md`

## 本次更新内容摘要
- Cloud Mail 账号列表“操作”列首个按钮从复制邮箱改为打开 `https://chatgpt.com/`。
- 调整邮箱、备注、创建时间、操作列宽度，给操作列留出稳定空间。
- 微调工具栏搜索框、配置卡片、表格单元格与备注区域间距，使页面更紧凑。
- 补记上一版 `RELEASE-2026-04-23-01.md` 的部署状态为“已部署”。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不涉及数据库结构变更，也不涉及迁移脚本调整。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.23-2`、确认服务器可拉取本次镜像，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 若镜像短暂未可拉取，必须继续自动重试，镜像可拉取后直接继续部署，不得中途停下。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 确认 `CloudMailAccountsView` 类型检查通过，生产构建成功。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.23-1`。
- 本次不涉及数据库结构变更，无需额外执行迁移回滚。

## 风险说明
- 本次变更会改变云邮账号页操作按钮语义，部署后应重点确认现场使用流程是否仍符合预期，避免用户误以为该按钮仍是“复制邮箱”。
- 页面表格列宽被压缩后，应重点回归窄屏或长备注场景，确认无明显截断或换行异常。

## 审核状态
- 待审核

## 部署状态
- 未部署
