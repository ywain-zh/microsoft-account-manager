# RELEASE-2026-04-22-07

## 本次发布日期
- 2026-04-22

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-7`

## 本次发布目的
- 微调 779 右侧接码短信区域的手机号视觉层级和间距。
- 让两个复制按钮更贴近手机号文本，减少右侧留白。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/views/Seven79CardsView.vue`
- `releases/RELEASE-2026-04-22-07.md`

## 本次更新内容摘要
- 右侧“专用接码线路”手机号字号略微调大，并让复制按钮更贴近手机号。
- PP 绑定线路下拉框宽度缩短，避免占用过长横向空间。
- PP 绑定线路下拉文字字号略微调大，提升可读性。
- PP 绑定线路复制按钮向左收紧，与下拉框距离更自然。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅调整 779 前端展示样式。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.22-7`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 若镜像暂未可用，必须继续等待并自动重试，镜像可用后直接继续部署，不得中途停下。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 确认专用接码线路手机号字体略大且复制按钮更贴近手机号。
- 确认 PP 绑定线路下拉框宽度缩短、文本更清晰、复制按钮更靠左。
- 确认窄屏下 PP 下拉框仍能自适应宽度。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-6`。
- 本次不涉及数据库结构变更，无需额外执行迁移回滚。

## 风险说明
- 本次主要为短信区域样式微调，部署后应重点回归手机号是否换行异常、复制按钮位置和窄屏排版。
- 如果后续手机号长度或状态文案发生变化，仍需关注下拉框缩短后是否出现截断过早的问题。

## 审核状态
- 审核通过

## 部署状态
- 已部署
