# RELEASE-2026-04-21-05

## 本次发布日期
- 2026-04-21

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.21-5`

## 本次发布目的
- 将 779 页面“获取session”按钮改为直接跳转到 ChatGPT session 接口页面，避免跨域读取失败提示误导用户。
- 将“镜像未就绪时持续等待并自动部署”的规则固化进部署文档。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `DEPLOY_LITE.md`
- `src/views/Seven79CardsView.vue`
- `releases/RELEASE-2026-04-21-02.md`
- `releases/RELEASE-2026-04-21-04.md`
- `releases/RELEASE-2026-04-21-05.md`

## 本次更新内容摘要
- 779 页面“获取session”按钮不再在当前站点发起跨域 fetch，改为直接新开页跳转到 `https://chatgpt.com/api/auth/session`。
- 删除已不再使用的 session 获取 loading 状态与跨域读取提示逻辑。
- 在部署文档中新增“镜像未就绪时的持续等待规则”，要求镜像未发布完成时持续轮询并在镜像可用后立即继续部署。
- 补记既有 release 文档的部署状态，保持发布记录与实际线上状态一致。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅调整前端交互与部署文档。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.21-5`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 若镜像暂未可用，必须继续等待并自动重试，镜像可用后直接继续部署，不得中途停下。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 确认“获取session”按钮点击后直接新开页访问 `https://chatgpt.com/api/auth/session`
- 确认 779 页面其余按钮与 PP 主流程构建无回归

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.21-4`。
- 本次不涉及数据库结构变更，无需额外执行数据迁移回滚。

## 风险说明
- 跳转后的 session 页面仍依赖用户在目标浏览器中已登录 `chatgpt.com`；若未登录，目标页仍会返回未登录结果。
- 部署文档中的持续等待规则会延长单次部署执行时长，但能避免镜像延迟发布时人工重复催促。

## 审核状态
- 审核通过

## 部署状态
- 未部署
