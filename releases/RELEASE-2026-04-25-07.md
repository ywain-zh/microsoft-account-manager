# RELEASE 2026-04-25-07

## 本次发布日期
- 2026-04-25

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-7`

## 本次发布目的
- 修复 `pay.py` 第 1463 行误留 `--` 导致 `IndentationError` 的问题。

## 本次来源分支
- `dev`
- 本次发布直接从 `dev` 发出，不涉及临时发布分支。

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `pay.py`
- `releases/RELEASE-2026-04-25-07.md`

## 本次更新内容摘要
- 删除 `pay.py` 支付流程中误留的 `--` 行，恢复 Python 语法正确性。
- 增加发布前 `python -m py_compile pay.py` 检查结果。

## 本次是否涉及配置变更
- 否
- 继续使用 `PAY_CONFIG_DEFAULT_PATH=/app/data/config.json`。
- 仅需部署固定镜像 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-7`。

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的后续升级部署步骤执行。
- 发布来源必须是 `dev`。
- 服务器只执行固定 tag 镜像 pull、compose up、状态检查、日志检查、资源检查和健康检查。

## 本次验证范围
- `python -m py_compile pay.py`
- `npm run typecheck`
- `npm run build`
- 部署后确认容器健康检查正常。

## 本次回滚方式
- 如部署后异常，将 `APP_IMAGE` 切回上一版已验证固定 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-6` 后执行 `docker compose pull` 和 `docker compose up -d`。
- 本次不涉及数据库迁移，不需要恢复数据库备份。

## 风险说明
- 本次只修复脚本语法错误，不改变支付流程和配置格式。

## 审核状态
- 用户运行 Stripe 支付脚本时报 `IndentationError`，已确认并修复。

## 部署状态
- 已部署
