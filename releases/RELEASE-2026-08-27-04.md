# RELEASE-2026-08-27-04

## 本次发布日期
- 2026-08-27

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.08.27-4`

## 本次发布目的
- 修复「添加 GLaDOS 账号」时会覆盖已有账号、无法新增的问题。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否。

## 本次是否涉及冲突处理
- 否。

## 本次修改文件列表
- `src/views/PublicCheckinView.vue`
- `releases/RELEASE-2026-08-27-04.md`

## 本次更新内容摘要

### 缺陷：新增 GLaDOS 账号会覆盖已有账号
- 现象：已有若干 GLaDOS 账号后，再点「添加账号」录入新账号并保存，新账号没有被创建，而是把之前某个已有账号的名称与 Cookie 整条替换掉。
- 排查：
  - 后端 `createAccount` 是纯 `INSERT`，`glados_checkin_accounts` 上没有唯一约束，也没有 upsert 语义，后端无问题。
  - 线上库确认 3 个 GLaDOS 账号（id 2/3/4）均存在，未丢账号；其中 id=4 的 `updated_at` 比 `created_at` 晚 278 秒，且 id=2/3 的 `updated_at` 相差 1 秒（属批量签到写入），说明只有 id=4 发生过一次「本应新增却变成更新」的手工写入。
- 根因：前端 `editingGladosAccount` 这个 ref 只在 `openEditGladosModal` 中被赋值，**没有任何路径把它复位为 `null`**。而 `submitGladosAccount` 正是依据该 ref 判断走新增还是更新分支。因此只要点过一次 GLaDOS 的「编辑」，此后每次「添加账号」都会静默走 `PUT` 更新，把那个账号覆盖掉。
- 同类根源：弹框复位逻辑此前被复制成三份（`openCreateModal` / `openPokemonModal` / `resetAccountModalContext`），新增 `editingGladosAccount` 时只有其中的赋值点被更新，复位点全部漏掉。公益站的 `editingAccount` 在三处都有复位，所以未受影响。
- 修复：
  - `resetAccountModalContext` 增加 `editingGladosAccount.value = null`。
  - 将 `resetAccountModalContext` 收敛为弹框的唯一复位入口，`openCreateModal` / `openPokemonModal` / `openEditModal` / `openEditGladosModal` 四个打开路径一律先调用它，再设置各自差异项，消除复制粘贴导致复位项漏更新的结构性隐患。

## 本次是否涉及配置变更
- 否。

## 本次是否涉及数据结构/迁移
- 否。纯前端改动，无新增迁移，无接口变更。

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的后续升级流程执行。
- 固定变量：
  - `IMAGE_TAG=2026.08.27-4`
  - `PREV_IMAGE_TAG=2026.08.27-3`
  - `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.08.27-4`
  - `PREV_APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.08.27-3`
  - `RELEASE_DOC=releases/RELEASE-2026-08-27-04.md`

## 本次验证范围
- `npm run typecheck` 通过；全量测试 100 项通过；`npm run build` 通过。
- 本仓库无前端组件测试设施（`tests/` 全部为 `node:test` 的服务端用例），该缺陷属于 Vue 组件状态问题，为此单独引入 vitest 不在本次修复范围内，改以 Playwright 对本地生产构建做真实回归。
- 回归脚本按用户实际操作路径驱动真实 UI：新增 acc-1/acc-2/acc-3 → 对 acc-3 点「编辑」后取消 → 再次「添加账号」录入 acc-4。
  - **修复前**：`[5:acc-1, 6:acc-2, 7:acc-3]` → `[5:acc-1, 6:acc-2, 7:acc-4]`，acc-3 被顶替，acc-4 未创建 —— 与用户反馈现象一致，确认脚本能复现。
  - **修复后**：`[8:acc-1, 9:acc-2, 10:acc-3, 11:acc-4]`，四个账号并存。
- 公益站路径回归：先编辑一个 GLaDOS 账号并取消制造状态污染，再新增 site-A/site-B、编辑 site-A 改名，结果为 `[2:site-A-renamed, 3:site-B]`，新增走新增、编辑走编辑，未受影响。

## 本次回滚方式
- 将服务器 `.env` 中 `APP_IMAGE` 切回 `ghcr.io/ywain-zh/microsoft-account-manager:2026.08.27-3`。
- 按 `DEPLOY_LITE.md` 执行 `docker compose pull`、`docker compose up -d`、日志与健康检查。
- 无迁移，回滚不需要恢复数据库。
- 注意：回滚会同时退回「添加 GLaDOS 账号会覆盖已有账号」的行为。

## 风险说明
- 纯前端改动，不涉及数据库、接口和外部依赖，风险面限于账号弹框的打开与复位。
- `openEditModal` / `openEditGladosModal` 现在会先执行一次完整复位再填入待编辑数据，行为等价，已通过上述回归覆盖。
- 历史上已被覆盖的账号无法自动还原。线上 id=4 的名称与 Cookie 是最后一次误更新写入的内容，如与预期不符需手工重新录入。

## 审核状态
- 审核通过。
- 用户已明确选择「提交并发布 2026.08.27-4」。

## 部署状态
- 已完成。

## 部署结果
- 发布提交：`676d3ef`（`fix: reset GLaDOS editing state when opening the account modal`）。
- GitHub Actions `Publish GHCR Image` 运行 `33259712843` 完成，结论为 `success`。
- 线上固定镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.08.27-4`。
- 部署前已备份 `.env.bak.2026.08.27-4` 与 `data/account-manager.db.bak.2026.08.27-4`（含 `-wal`、`-shm`）。
- 补充说明：`releases/RELEASE-2026-08-27-0{1,2,3,4}.md` 此前未同步到服务器，`DEPLOY_LITE.md` 的 `test -f "$RELEASE_DOC"` 检查在前三次部署被跳过，本次已一并补齐到 `/opt/microsoft-account-manager/releases/`。
- 容器状态为 `running healthy`；`/api/health` 返回 `{"ok":true}`。
- 无迁移需要执行；存量数据核对：GLaDOS 3 个账号（id 2/3/4）、公益站 10 个、宝可梦 4 个，全部完好。
- 部署后资源抽查：CPU `0.09%`，容器内存约 `79.16 MiB / 512 MiB`。
- 清理前根分区占用 `48%`，清理后 `47%`；已删除旧镜像 `2026.08.27-2`。
- 服务器仅保留当前镜像 `2026.08.27-4` 与回滚镜像 `2026.08.27-3`，无需回滚。
- **缺陷修复线上确认**：从 `2026.08.27-3` 与 `2026.08.27-4` 两个镜像中分别取出 `PublicCheckinView` chunk 比对，旧版打开新增弹框的函数为 `function fi(){he.value=null,pe.value="public",...}`（只复位 `editingAccount` 一个 ref）；新版为 `function hi(){an(),$e.value=!0}` 且 `function an(){me.value=null,F.value=null,...}`（`editingAccount` 与 `editingGladosAccount` 两个 ref 均复位）。确认线上运行的是修复后的代码。未在生产库中创建任何测试账号。
