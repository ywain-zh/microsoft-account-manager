# Release 2026.05.18-4

## 本次发布日期
- 2026-05-18

## 本次发布版本/tag
- `2026.05.18-4`
- 镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.18-4`

## 本次发布目的
- 调整微软邮箱列表工具栏按钮顺序、文案和颜色，使刷新、导入、RF 刷新和批量删除操作更清晰。

## 本次修改文件列表
- `src/views/AccountsView.vue`

## 本次更新内容摘要
- 去掉微软邮箱工具栏中的“全选”和“反选”按钮。
- 将“刷新列表”改为“刷新”，并放在工具栏第一个。
- 将“刷新选中”改为“刷新RF”，将“刷新全部”改为“批量刷新RF”。
- 将 RF 刷新按钮移动到“批量导入”后方。
- 将“批量删除”移动到最后，并改为红色实心危险按钮。
- 为“刷新RF”和“批量刷新RF”增加绿色/橙色区分。

## 本次是否涉及配置变更
- 不涉及。

## 本次是否涉及数据结构/迁移
- 不涉及。

## 本次部署步骤
- 已本地执行 `npm run typecheck`。
- 已本地执行 `npm run build`。
- 推送 `dev` 分支和固定 tag `2026.05.18-4`，等待 GHCR 镜像构建完成。
- 服务器侧按 `DEPLOY_LITE.md` 执行固定镜像拉取、`.env` 备份、`APP_IMAGE` 更新、`docker compose pull`、`docker compose up -d`、日志与健康检查。

## 本次回滚方式
- 将服务器 `.env` 中 `APP_IMAGE` 切回上一版已验证镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.18-3`。
- 执行 `docker compose pull` 与 `docker compose up -d`。

## 风险说明
- 本次仅调整前端按钮展示与顺序，不改变后端刷新逻辑。

## 审核通过
- 用户已明确要求“部署下”。

## 部署状态
- 已部署

## 实际部署记录
- 部署服务器：`aliyun-server:/opt/microsoft-account-manager`
- 部署镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.18-4`
- 已完成 `.env` 备份并将 `APP_IMAGE` 从 `2026.05.18-3` 更新到 `2026.05.18-4`。
- 已执行 `docker compose pull` 与 `docker compose up -d`。
- 容器状态：`Up (healthy)`。
- 容器内健康检查：`{"ok":true}`。
- 外网健康检查：`http://47.251.82.144:8787/api/health` 返回 `{"ok":true}`。
- 部署后资源占用：CPU 约 `0.34%`，内存约 `21.46MiB / 512MiB`。
