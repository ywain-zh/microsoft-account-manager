# RELEASE-2026-04-22-08

## 本次发布日期
- 2026-04-22

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-8`

## 本次发布目的
- 统一右侧两个接码手机号的展示格式与字号。
- 修正卡片状态区“过期”左侧多余的黄色阴影。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/views/Seven79CardsView.vue`
- `releases/RELEASE-2026-04-22-08.md`

## 本次更新内容摘要
- 专用接码线路手机号改成与 PP 接码一致的 `(+区号) 号码` 展示格式。
- 专用接码线路复制按钮改为仅复制号码主体，不再复制区号。
- 右侧两个手机号字号统一为同一尺寸。
- PP 接码下拉框长度缩短到更贴近设计截图。
- 去掉“过期”状态左侧状态点的黄色外扩阴影。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅调整 779 前端展示样式与复制交互。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.22-8`、确认服务器可拉取本次镜像，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 若镜像短暂未可拉取，必须继续自动重试，镜像可拉取后直接继续部署，不得中途停下。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 确认专用接码线路与 PP 接码手机号都显示为 `(+1) 号码` 格式。
- 确认两个复制按钮都只复制号码主体，不复制区号。
- 确认两个手机号字号一致，PP 下拉框长度符合设计截图。
- 确认“过期”左侧不再出现黄色阴影。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-7`。
- 本次不涉及数据库结构变更，无需额外执行迁移回滚。

## 风险说明
- 本次主要为短信区域展示与状态点样式微调，部署后应重点回归手机号格式、复制内容和状态 badge 视觉表现。
- 如果后续接入非 +1 号码或更长状态文案，仍需关注展示长度与格式拆分是否符合预期。

## 审核状态
- 审核通过

## 部署状态
- 未部署
