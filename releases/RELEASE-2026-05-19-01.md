# RELEASE-2026-05-19-01

## 本次发布日期
- 2026-05-19

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.19-1`

## 本次发布目的
- 修复部分 Hotmail/Outlook 账号自动取件时 Graph token 刷新失败的问题。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/app.ts`
- `releases/RELEASE-2026-05-19-01.md`

## 本次更新内容摘要
- 将 Graph 取件刷新 token 的 scope 从 `https://graph.microsoft.com/.default` 改为显式 delegated scope：`https://graph.microsoft.com/Mail.Read offline_access`。
- 线上复现确认 `EdenDonald9112@hotmail.com` 使用 `.default` 会返回 `AADSTS90023`，改用显式 `Mail.Read` 后可成功换取 Graph access token 并读取 inbox 邮件。

## 本次是否涉及配置变更
- 否

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 中的标准流程执行。
- 本地验证通过后提交 `dev`，推送 tag `2026.05.19-1` 触发 GHCR 镜像构建。
- GHCR 固定 tag 镜像可用后，服务器侧仅执行固定 tag 镜像拉取、`.env` 备份、`APP_IMAGE` 更新、`docker compose pull`、`docker compose up -d`、日志与健康检查。

## 本次验证范围
- 线上只读复现：`.default` 返回 `AADSTS90023`。
- 线上只读复现：`https://graph.microsoft.com/Mail.Read offline_access` 可成功读取 `EdenDonald9112@hotmail.com` inbox。
- `npm run typecheck`
- `npm run build`

## 本次回滚方式
- 按 `DEPLOY_LITE.md` 回滚到上一版已验证 tag：`2026.05.18-4`。
- 本次不涉及数据库迁移，通常无需恢复数据库备份。

## 风险说明
- 该改动只影响 Graph 取件前的 refresh token scope。
- OAuth 登录原本已经申请 `Mail.Read`，手动导入的 token 如本身没有 Graph Mail.Read 权限，仍会按微软返回结果失败。

## 审核状态
- 审核通过：用户要求直接在线排查并修复。

## 部署状态
- 已部署

## 实际部署记录
- 部署服务器：`aliyun-server:/opt/microsoft-account-manager`
- 固定镜像：`ghcr.io/ywain-zh/microsoft-account-manager:2026.05.19-1`
- 已同步 release 文档：`/opt/microsoft-account-manager/releases/RELEASE-2026-05-19-01.md`
- 已备份 `.env`：`/opt/microsoft-account-manager/.env.bak.2026.05.19-1`
- `docker compose pull` 与 `docker compose up -d` 执行成功。
- 健康检查返回：`{"ok":true}`
- 容器状态：`microsoft-account-manager` 为 `running healthy`
- 线上复测账号 `EdenDonald9112@hotmail.com`：Graph 取件成功，共 3 封邮件。
