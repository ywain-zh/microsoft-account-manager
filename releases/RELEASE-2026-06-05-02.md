# RELEASE 2026-06-05-02

## 本次发布日期
- 2026-06-05

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.05-2`

## 本次发布目的
- 修复 hosted 模式下上游只返回 `chatgpt.com/checkout/...` 时，页面误把 session 地址当作支付长链展示的问题。

## 本次来源分支
- `dev`

## 本次修改文件列表
- `server/runtime/sub2api-long-link.ts`
- `server/runtime/sub2api-long-link-helper.py`
- `server/runtime/sub2api-long-link-source/server.py`
- `tests/sub2api-long-link.test.ts`
- `releases/RELEASE-2026-06-05-02.md`

## 本次更新内容摘要
- hosted checkout 返回缺少 `openai_payurl` 时，根据 `checkout_session_id` 合成 `https://pay.openai.com/c/pay/{session_id}?ui_mode=hosted`。
- Python helper 在返回中保留本次 `checkout_ui_mode`，避免 custom 模式和 hosted 模式展示混淆。
- TS API 归一化层增加同样兜底，确保页面优先复制/打开 OpenAI Pay 长链。
- 增加单测覆盖上游只返回 `chatgpt.com/checkout/openai_llc/cs_live...` 的场景。

## 本次验证范围
- `npm run typecheck`
- `node --experimental-strip-types --test tests\sub2api-long-link.test.ts tests\sub2api-oauth.test.ts`
- `py -3 -m py_compile server\runtime\sub2api-long-link-helper.py server\runtime\sub2api-long-link-source\server.py`
- `npm run build`

## 本次回滚方式
- 回滚到上一版已验证 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.05-1`。
- 按 `DEPLOY_LITE.md` 回滚步骤将 `.env` 中 `APP_IMAGE` 切回上一版固定 tag，然后执行 `docker compose pull` 和 `docker compose up -d`。

## 风险说明
- 本次只调整 hosted 返回链接映射，不改变 checkout 请求参数和代理链路。
- 如果 `pay.openai.com/c/pay/{session_id}?ui_mode=hosted` 被上游调整，仍需根据上游最新跳转规则更新。

## 审核状态
- 审核通过（线上验证发现 hosted 展示链接不符合预期，按热修复发布）

## 部署状态
- 未部署
