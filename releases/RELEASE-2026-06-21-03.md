# RELEASE 2026-06-21-03

## 本次发布日期
- 2026-06-21

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-3`

## 本次发布目的
- 发布公益站模型列表空结果友好提示，避免无模型时显示 `请求失败 (502)`。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/runtime/public-checkin.ts`
- `src/views/PublicCheckinView.vue`
- `tests/public-checkin.test.ts`

## 本次更新内容摘要
- 公益站模型接口在上游成功返回但没有模型时，返回 `200` 和空 `items`，不再抛出 `502`。
- 空模型结果不写入模型缓存，避免临时空结果被缓存。
- 前端模型弹框保持打开，并显示提示：`未读取到可用模型。该站点暂时没有返回模型列表，请确认 API Key 有模型权限，或稍后再试。`
- 真实请求失败、API Key 无效、超时等错误仍按原错误逻辑提示。
- 补充模型空结果与排序响应构建的回归测试。

## 本次是否涉及配置变更
- 否。

## 本次是否涉及数据结构/迁移
- 否。

## 本次部署步骤
- 必须严格执行 `DEPLOY_LITE.md` 的后续升级部署步骤。
- 发布来源固定为 `dev`。
- 服务器只允许拉取固定镜像 tag 并重启容器，不允许在服务器构建。

## 本次验证范围
- `node --test tests/public-checkin.test.ts`
- `npm run typecheck`
- `npm run build`

## 本次回滚方式
- 按 `DEPLOY_LITE.md` 回滚步骤切回上一版已验证镜像：
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-2`
- 本次不涉及数据库结构变更，回滚不需要恢复数据库结构。

## 风险说明
- 本次只改变模型列表为空时的响应语义和前端空态提示，不改变 API Key 保存、模型读取鉴权和真实错误处理。
- 空模型结果不会缓存，用户再次点击模型会重新请求上游。

## 审核状态
- 用户已明确要求“部署下”，视为审核通过并批准部署。

## 部署状态
- 已部署

## 部署结果
- GitHub Actions `Publish GHCR Image` 已成功构建并推送镜像，运行 ID：`27893530141`。
- 服务器已同步 release 文档到 `/opt/microsoft-account-manager/releases/RELEASE-2026-06-21-03.md`。
- 服务器侧 `docker manifest inspect ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-3` 校验通过。
- 服务器 `/opt/microsoft-account-manager/.env` 已更新为 `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-3`。
- 已创建部署前备份：
  - `.env.bak.2026.06.21-3`
  - `data/backups/account-manager.2026.06.21-3.<timestamp>.db`
- 已执行 `docker compose pull` 和 `docker compose up -d`，未在服务器构建。
- 当前容器 `microsoft-account-manager` 使用镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-3`，状态为 `running healthy`。
- 容器内健康检查 `http://127.0.0.1:8787/api/health` 返回 `{"ok":true}`。
- 线上公网健康检查 `https://mail.0222999.xyz/api/health` 返回 HTTP 200 和 `{"ok":true}`。
- 线上首页 `https://mail.0222999.xyz/` 返回 HTTP 200，页面标题为 `望月工具箱`。
- 容器资源占用正常，部署后内存约 `76.07MiB / 512MiB`。
- 已清理旧项目镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-1`。
- 当前保留镜像：
  - 当前版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-3`
  - 回滚版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-2`
- 清理前根分区约 `29%` 使用率，清理后约 `28%` 使用率。
- 未执行回滚。
