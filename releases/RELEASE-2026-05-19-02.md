# RELEASE-2026-05-19-02

## 本次发布日期
- 2026-05-19

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.19-2`

## 本次发布目的
- 修复 `2026.05.19-1` 对部分历史微软邮箱 token 的兼容性回归。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/app.ts`
- `releases/RELEASE-2026-05-19-02.md`

## 本次更新内容摘要
- Graph 取件刷新 token 时改为双 scope 兼容策略。
- 先尝试 `https://graph.microsoft.com/Mail.Read offline_access`，兼容 `EdenDonald9112@hotmail.com` 这类 `.default` 会失败的账号。
- 如果显式 `Mail.Read` 返回未授权或过期，再自动回退 `https://graph.microsoft.com/.default`，兼容历史导入 token。

## 本次是否涉及配置变更
- 否

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 中的标准流程执行。
- 本地验证通过后提交 `dev`，推送 tag `2026.05.19-2` 触发 GHCR 镜像构建。
- GHCR 固定 tag 镜像可用后，服务器侧仅执行固定 tag 镜像拉取、`.env` 备份、`APP_IMAGE` 更新、`docker compose pull`、`docker compose up -d`、日志与健康检查。

## 本次验证范围
- 线上只读验证：`MaizieDanny2891@hotmail.com` 显式 `Mail.Read` 失败，但 `.default` 成功并可读取 Graph inbox。
- `npm run typecheck`
- `npm run build`

## 本次回滚方式
- 按 `DEPLOY_LITE.md` 回滚到上一版已验证 tag：`2026.05.18-4`。
- 不建议回滚到 `2026.05.19-1`，因为该版本对历史 token 有兼容性回归。

## 风险说明
- 本次只调整 Graph 取件前的 refresh token scope 尝试顺序。
- 如果一个账号两个 Graph scope 都失败，仍会继续按原流程尝试 IMAP。

## 审核状态
- 审核通过：用户反馈其他账号不可用，要求立即修复。

## 部署状态
- 未部署
