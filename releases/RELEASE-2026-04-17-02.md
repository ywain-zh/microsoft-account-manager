# RELEASE-2026-04-17-02

## 本次发布日期
- 2026-04-17

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.17-2`

## 本次发布目的
- 修复微软邮箱收件箱中“已读邮件仍显示未读”的问题，避免用户打开邮件后状态与实际阅读情况不一致。
- 同步统一 Cloud Mail 收件箱的本地已读交互，保持两套收件箱体验一致。

## 本次修改文件列表
- `server/app.ts`
- `src/state/admin-console.ts`
- `src/state/cloud-mail-console.ts`
- `src/views/AccountsView.vue`
- `src/views/CloudMailAccountsView.vue`
- `releases/RELEASE-2026-04-17-02.md`

## 本次更新内容摘要
- 放宽微软邮件读状态解析规则，兼容 `true/false`、`1/0`、`read/unread`、`seen/new` 等返回值，避免服务端把已读状态漏判为空。
- 微软邮箱收件箱在用户点击邮件后，会立即把当前邮件本地状态切换为已读，避免界面继续显示“未读”。
- Cloud Mail 收件箱同步补上相同的本地已读切换逻辑，保持收件箱交互一致。
- 不调整邮件拉取接口、页面结构或其他业务逻辑，仅修正已读/未读状态映射与展示同步。

## 本次是否涉及配置变更
- 否
- 本次不新增环境变量，不修改部署参数，仅需要将 `APP_IMAGE` 更新为本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不涉及数据库结构变更、迁移脚本和数据导入流程。

## 本次部署步骤
- 严格执行 `DEPLOY_LITE.md` 中的标准流程：本地验证 -> release 文档 -> 推送代码 -> push tag 发镜像 -> 确认 GHCR tag 镜像可用 -> 服务器 pull/up -> 健康检查。
- 服务器侧只允许执行文档中的检查命令、`docker compose pull`、`docker compose up -d`、日志检查、资源检查和健康检查命令。
- 禁止在服务器执行 `docker build`、`docker compose up -d --build`、`npm install`、`npm run build`、`vite build`。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.17-1`。
- 本次发布不涉及数据库迁移，如仅回退读状态展示修复，无需恢复数据库备份。

## 风险说明
- 本次风险主要集中在读状态判断逻辑，需要重点观察微软邮箱接口在不同账号模式下返回的已读字段是否都能被正确识别。
- 前端新增了“点击即本地标记已读”的同步逻辑，部署后需重点确认刷新收件箱后状态不会反复跳变。
- 部署前必须确认 GHCR 中已生成固定 tag `2026.04.17-2` 的镜像，否则立即停止，不允许进入服务器升级步骤。

## 审核状态
- 审核通过

## 部署批准状态
- 已批准

## 部署环境
- dev

## 部署状态
- 已部署

## 部署记录
- 部署时间：`2026-04-17 18:15:09 +08:00`
- 部署分支：`dev`
- 部署提交：`80e0106a8a537fb1aa2de49f4960176fb2bd8cc1`
- 发布 tag：`2026.04.17-2`
- 服务器目录：`/opt/microsoft-account-manager`
- 环境备份：`.env.bak.2026.04.17-2`
- 发布前已通过远端 `docker manifest inspect ghcr.io/ywain-zh/microsoft-account-manager:2026.04.17-2` 确认 GHCR 固定 tag 镜像可用。
- 服务器按 `DEPLOY_LITE.md` 执行 `docker compose pull` 与 `docker compose up -d`，未在服务器执行任何构建命令。

## 部署后验证结果
- 本地验证通过：`npm run typecheck`、`npm run build`
- 容器状态正常：`microsoft-account-manager` 为 `running healthy`
- 容器内健康检查正常：`http://127.0.0.1:8787/api/health` 返回 `{"ok":true}`
- 服务器本机健康检查正常：`http://127.0.0.1:8787/api/health` 返回 `{"ok":true}`
- 部署后 `.env` 已指向固定镜像 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.17-2`
