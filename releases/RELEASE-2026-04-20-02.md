# RELEASE-2026-04-20-02

## 本次发布日期
- 2026-04-20

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.20-2`

## 本次发布目的
- 修正 779 工作台默认预填卡密的问题，页面打开时不再自动带示例卡密。
- 修正 779 卡片有效期计算逻辑，改为基于卡密有效期再顺延 5 小时，而不是基于当前时间固定加时。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/views/Seven79CardsView.vue`
- `server/app.ts`
- `releases/RELEASE-2026-04-20-02.md`

## 本次更新内容摘要
- 将 779 页面顶部卡密输入框默认值改为空字符串。
- 将 `card_valid_until` 的写入逻辑从 `CURRENT_TIMESTAMP + 6 hours` 改为 `check.expiryTime + 5 hours`。
- 为卡密有效期解析增加空值/非法时间保护，解析失败时写入 `NULL`，避免前端出现异常倒计时。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅调整已有字段 `card_valid_until` 的计算来源。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.20-2`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。

## 本次验证范围
- `vue-tsc --noEmit`
- `tsc -p tsconfig.server.json --noEmit`
- `vite build`
- `esbuild server/main.ts server/cli/migrate.ts server/cli/import-legacy.ts --bundle --platform=node --format=esm --target=node22 --external:better-sqlite3 --outdir=build/server --outbase=server --entry-names=[dir]/[name]`
- 本地检查 779 页面默认输入为空
- 验卡后确认 `cardValidUntil = checkExpiryTime + 5 hours`

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.20-1`。
- 本次不涉及数据库结构变更，无需额外执行数据迁移回滚。

## 风险说明
- 重点观察远端 `expiry_time` 字段格式是否始终稳定；若第三方返回异常时间字符串，本次逻辑会把 `card_valid_until` 写为 `NULL`。
- 重点核对历史页和工作台顶部剩余时间是否与新的 +5 小时规则一致。

## 审核状态
- 审核通过

## 部署状态
- 未部署
