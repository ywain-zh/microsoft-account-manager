# 2026-06-26-01 Release

## 本次发布日期
- 2026-06-26

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.26-1`

## 本次发布目的
- 调整 Linux DO 邮箱左侧导航，将写邮件、收件箱、发件箱统一收纳到同一个操作卡片中。
- 修正三项入口的文案列对齐，让写邮件、收件箱、发件箱在视觉上保持水平一致。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `src/views/LinuxDoMailView.vue`
- `releases/RELEASE-2026-06-26-01.md`

## 本次更新内容摘要
- 将 Linux DO 邮箱侧栏中的写邮件、收件箱、发件箱统一放入 `gmail-rail-action-card` 卡片。
- 移除顶部工具栏中旧的发件按钮入口，统一使用左侧写邮件入口。
- 将发件文案统一调整为写邮件。
- 统一三项入口的网格列、图标宽度、边框盒模型、行高和溢出规则，保证三项文案左边缘对齐。
- 移动端保持同样的 8px 卡片圆角，不再回退到旧的胶囊形文件夹按钮。

## 本次是否涉及配置变更
- 是。线上镜像 tag 需要更新为 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.26-1`。
- 不新增 `.env` 配置项。

## 本次是否涉及数据结构/迁移
- 否。本次仅涉及前端页面结构与样式，不新增数据库迁移。

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的标准发布与后续升级部署流程执行。
- 发布来源固定为 `dev`。
- 服务器仅拉取固定镜像 tag 并重启容器，不在服务器构建。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 本地生产服务部署到 `http://127.0.0.1:8787/` 后，首页返回 HTTP 200。
- 未登录状态下 `/api/auth/session` 返回 HTTP 401 与 `未登录或登录已过期`，符合预期。

## 本次回滚方式
- 将服务器 `.env` 中 `APP_IMAGE` 切回当前线上已验证镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.25-4`。
- 执行 `docker compose pull && docker compose up -d`。
- 本次不新增迁移，通常不需要恢复数据库。

## 风险说明
- 本次只调整 Linux DO 邮箱页面的左侧入口布局，主要风险是侧栏视觉间距或移动端布局显示不符合预期。
- 不涉及后端接口、鉴权、邮件收发逻辑和数据结构。
- 若上线后发现 UI 回归，可直接回滚至 `2026.06.25-4`。

## 审核状态
- 审核通过
- 用户已明确要求“根据部署文档，部署到阿里云线上服务器”，视为批准本次部署。

## 部署状态
- 已部署

## 部署结果
- GitHub Actions `Publish GHCR Image` 已成功构建并推送镜像，运行 ID：`28226847529`。
- 服务器已同步 release 文档到 `/opt/microsoft-account-manager/releases/RELEASE-2026-06-26-01.md`。
- 服务器侧 `docker manifest inspect ghcr.io/ywain-zh/microsoft-account-manager:2026.06.26-1` 校验通过。
- 服务器 `/opt/microsoft-account-manager/.env` 已更新为 `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.06.26-1`。
- 已创建部署前环境备份：`.env.bak.2026.06.26-1`。
- 已执行 `docker compose pull` 和 `docker compose up -d`，未在服务器构建。
- 当前容器 `microsoft-account-manager` 使用镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.26-1`，状态为 `running healthy`。
- 容器内健康检查 `http://127.0.0.1:8787/api/health` 返回 `{"ok":true}`。
- 线上公网健康检查 `https://mail.0222999.xyz/api/health` 返回 HTTP 200 和 `{"ok":true}`。
- 线上首页 `https://mail.0222999.xyz/` 返回 HTTP 200，页面标题为 `望月工具箱`。
- 容器资源占用正常，部署后内存约 `63.01MiB / 512MiB`。
- 服务器内存状态正常：`1.6Gi` 总内存，约 `748Mi` 可用，Swap 未使用。
- 旧镜像清理已完成：删除 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.25-3`。
- 当前项目镜像仅保留：
  - 当前版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.26-1`
  - 回滚版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.25-4`
- 清理前根分区约 `25%` 使用率，清理后约 `23%` 使用率。
- 清理前 Docker 镜像占用约 `2.868GB`，清理后约 `2.414GB`。
- 未执行回滚。
