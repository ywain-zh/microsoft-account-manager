# RELEASE 2026-06-21-05

## 本次发布日期
- 2026-06-21

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-5`

## 本次发布目的
- 发布全局密钥/密码输入明文展示，以及公益站添加账号弹窗防浏览器自动填充优化。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `src/components/SecretInput.vue`
- `src/views/LoginView.vue`
- `src/views/PublicCheckinView.vue`

## 本次更新内容摘要
- 全局 `SecretInput` 固定使用明文输入，移除密码圆点、眼睛按钮和隐藏/显示状态。
- 登录页密码框改为明文输入，登录成功后仍清空表单密码值。
- 公益站添加/编辑账号弹窗增加隐藏 autofill guard，降低浏览器把登录账号密码自动填入站点 URL、User ID 等业务字段的概率。
- 公益站账号表单为站点名称、站点 URL、Key、User ID、模型 API Key 设置独立 `name` 和 `autocomplete` 属性。

## 本次是否涉及配置变更
- 否。

## 本次是否涉及数据结构/迁移
- 否。

## 本次部署步骤
- 必须严格执行 `DEPLOY_LITE.md` 的后续升级部署步骤。
- 发布来源固定为 `dev`。
- 服务器只允许拉取固定镜像 tag 并重启容器，不允许在服务器构建。

## 本次验证范围
- `node --test tests/public-checkin.test.ts`
- `node --test tests/notifications.test.ts`
- `npm run typecheck`
- `npm run build`
- `git diff --check`

## 本次回滚方式
- 按 `DEPLOY_LITE.md` 回滚步骤切回上一版已验证镜像：
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-4`
- 本次不涉及数据库结构变更，回滚不需要恢复数据库结构。

## 风险说明
- 本次按个人使用偏好取消前端遮罩，已登录管理端页面会直接显示密钥/密码内容。
- 存储方式和接口结构不变，不新增数据库字段。
- 浏览器自动填充行为由浏览器/密码管理器控制，本次通过 guard 和字段属性降低误填概率，但个别扩展仍可能自行填充。

## 审核状态
- 用户已明确要求“根据部署文档部署”，视为审核通过并批准部署。

## 部署状态
- 已部署

## 部署结果
- GitHub Actions `Publish GHCR Image` 已成功构建并推送镜像，运行 ID：`27905702977`。
- 服务器已同步 release 文档到 `/opt/microsoft-account-manager/releases/RELEASE-2026-06-21-05.md`。
- 服务器侧 `docker manifest inspect ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-5` 校验通过。
- 服务器 `/opt/microsoft-account-manager/.env` 已更新为 `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-5`。
- 已创建部署前备份：
- `.env.bak.2026.06.21-5`
- `data/backups/account-manager.2026.06.21-5.20260621212619.db`
- 已执行 `docker compose pull` 和 `docker compose up -d`，未在服务器构建。
- 当前容器 `microsoft-account-manager` 使用镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-5`，状态为 `running healthy`。
- 容器内健康检查 `http://127.0.0.1:8787/api/health` 返回 `{"ok":true}`。
- 线上公网健康检查 `https://mail.0222999.xyz/api/health` 返回 HTTP 200 和 `{"ok":true}`。
- 线上首页 `https://mail.0222999.xyz/` 返回 HTTP 200。
- 容器资源占用正常，部署后内存约 `67.93MiB / 512MiB`。
- 已清理旧项目镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-3`。
- 当前保留镜像：
- 当前版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-5`
- 回滚版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-4`
- 清理前根分区约 `29%` 使用率，清理后约 `28%` 使用率。
- 未执行回滚。
