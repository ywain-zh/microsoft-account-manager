# RELEASE 2026-09-01-02

## 本次发布日期
- 2026-09-01

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.09.01-2`

## 本次发布目的
- 发布 dian115 签到账号类型功能（上个开发周期完成、未发布）。
- 签到页面 UI 整改：去除顶部统计卡片、统一列表行样式、修复 dian115 表单单选组件不渲染问题、账号类型标签（GL/D1）重新定位。
- 建立版本号体系：package.json 从 `0.1.0` 升到 `1.0.0`，侧边栏版本号改为动态读取 package.json，后续版本号与发布联动。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `package.json`（版本号 0.1.0 → 1.0.0）
- `src/layouts/AdminShell.vue`（侧边栏版本号动态读取 package.json）
- `src/style.css`（版本号样式从 CSS 伪元素硬编码改为真实元素类 `.console-brand-version`）
- `src/views/PublicCheckinView.vue`（UI 整改 + 补导入 NRadio/NRadioGroup + GL/D1 标签前置）
- `server/runtime/dian115-checkin.ts`（新增：dian115 签到适配器与调度）
- `server/app.ts`、`server/runtime/public-checkin.ts`（dian115 路由与调度接入）
- `src/api.ts`、`src/types.ts`（dian115 前端 API 与类型）
- `migrations/0026_dian115_checkin.sql`、`migrations/0027_dian115_password_auth.sql`（dian115 数据表）
- `tests/dian115-checkin.test.ts`（单元测试，16 例全过）

## 本次更新内容摘要
- 新增 dian115 签到账号类型：支持邮箱密码 / Cookie 两种登录方式、普通签到 / 运气签到两种模式、自动签到调度与积分余额刷新。
- 签到页顶部 4 个统计卡片（总账号数/启用签到/今日成功/今日奖励）移除。
- 列表行样式统一：移除宝可梦/GLaDOS/dian115 行的彩色高亮背景与左侧竖条，与公益站普通行一致。
- 修复 dian115 表单「登录方式/签到模式」单选组件因缺少 naive-ui 导入而不渲染、无法选择的问题；删除表单内 4 处说明文案；登录方式与签到模式并排布局，单选胶囊化样式。
- GL/D1 类型标签移至账号名称前方同一行，flex 布局防止换行错位。
- 版本号规范：起始 `1.0.0`，小版本 `1.0.1`，大版本 `1.1.1`，改版 `2.0.0`；侧边栏「望月工具箱」下方版本徽标与 package.json 联动。

## 本次是否涉及配置变更
- 否（无需新增环境变量；`APP_IMAGE` 按标准流程更新为新 tag）

## 本次是否涉及数据结构/迁移
- 是。新增迁移 `0026_dian115_checkin.sql`（dian115 账号表）与 `0027_dian115_password_auth.sql`（密码登录字段）。服务启动时自动执行，纯新增表/字段，不影响既有数据；回滚方式为恢复升级前备份的 SQLite 文件。

## 本次部署步骤
- 按 `DEPLOY_LITE.md`「后续升级部署步骤」执行：服务器侧 `cp .env .env.bak` → 更新 `APP_IMAGE` 为 `2026.09.01-2` → `docker compose pull` → `docker compose up -d` → 状态/日志/健康检查 → 清理旧镜像（保留 `2026.09.01-1` 作为回滚 tag）。

## 本次验证范围
- `npm run typecheck`（vue-tsc + tsc 零错误）
- `npm run build`（web + server 构建成功）
- `node --test tests/dian115-checkin.test.ts`（16/16 通过）
- 本地 dev 环境浏览器实测：签到页统计卡片移除、四类账号行样式统一、dian115 编辑弹窗单选可选、GL/D1 标签位置正常、版本号显示 v1.0.0

## 本次回滚方式
- 切回上一版已验证 tag `ghcr.io/ywain-zh/microsoft-account-manager:2026.09.01-1`；如需回滚数据，恢复升级前备份的 `data/account-manager.db`。

## 风险说明
- dian115 为新功能首次上线，站点由 Cloudflare 保护，签到可能被风控拦截；建议上线后观察签到日志。
- 本次含数据库迁移（纯新增），回滚需配合数据库备份。
- 版本号为首次发布 1.0.0，无历史兼容问题。

## 审核状态
- 审核通过（用户在会话中明确指示按部署文档执行）

## 部署状态
- 已部署

## 部署结果记录（2026-09-01）
- 服务器：`aliyun-server`（47.251.82.144），部署目录 `/opt/microsoft-account-manager`
- 执行：`.env` 备份为 `.env.bak.2026.09.01-2` → 更新 `APP_IMAGE` 为 `2026.09.01-2` → `docker compose pull` → `docker compose up -d`
- 容器状态：`Up (healthy)`，内存 64MiB / 512MiB，CPU 正常
- 迁移：启动时自动执行 `0026_dian115_checkin.sql`、`0027_dian115_password_auth.sql`
- 健康检查：`/api/health` 返回 `{"ok":true}`
- 旧镜像清理：删除 `2026.08.27-4`，保留 `2026.09.01-2`（当前）与 `2026.09.01-1`（回滚）；磁盘 48% → 47%
- 线上验证：AdminShell 分包内含 `v1.0.0` 版本徽标