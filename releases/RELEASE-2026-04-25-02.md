# RELEASE 2026-04-25-02

## 本次发布日期
- 2026-04-25

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-2`

## 本次发布目的
- 修复 779 激活码有效期在 UTC 容器环境下未按北京时区显示的问题。

## 本次来源分支
- `dev`
- 本次发布直接从 `dev` 发出，不涉及临时发布分支。

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/app.ts`
- `releases/RELEASE-2026-04-25-02.md`

## 本次更新内容摘要
- 将服务端 779 日期时间格式化固定为 `Asia/Shanghai`。
- 避免容器系统时区为 UTC 时，把 779 返回的 ISO UTC 时间直接显示成 UTC。
- 779 激活码有效期 `2026-04-24T17:24:26.078Z` 应显示为北京时间 `2026-04-25 01:24:26`。

## 本次是否涉及配置变更
- 否
- 仅需部署固定镜像 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-2`。

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的后续升级部署步骤执行。
- 发布来源必须是 `dev`。
- 服务器只执行固定 tag 镜像 pull、compose up、状态检查、日志检查、资源检查和健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 线上检查容器时区为 UTC，Node `Asia/Shanghai` 格式化正常。

## 本次回滚方式
- 如部署后异常，将 `APP_IMAGE` 切回上一版已验证固定 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-1` 后执行 `docker compose pull` 和 `docker compose up -d`。
- 本次不涉及数据库迁移，不需要恢复数据库备份。

## 风险说明
- 本次调整影响使用 `formatSqliteDateTime` 格式化的服务端日期时间展示。
- 需要部署后重点检查 779 激活码有效期是否显示为北京时间。

## 审核状态
- 审核通过

## 部署状态
- 未部署
