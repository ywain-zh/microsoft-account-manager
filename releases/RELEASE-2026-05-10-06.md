# Release 2026.05.10-6

## 本次发布日期
- 2026-05-10

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.10-6`

## 本次发布目的
- 修正微软邮箱“独立 `client_secret` 兼容”上线后的回退参数遗漏问题，确保商家四段格式账号与系统原生 OAuth 账号都能正常取件。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/app.ts`
- `releases/RELEASE-2026-05-10-06.md`

## 本次更新内容摘要
- 为微软邮箱刷新与取件调用链补传 `MS_CLIENT_ID`，修复全局 secret 回退判断失效的问题。
- 兼容两种账号来源：
  - 商家导入的四段格式账号：`账号----密码----client_id----refresh_token`
  - 系统原生 OAuth 登录账号：继续使用全局 `MS_CLIENT_SECRET`
- 保持上一版新增的独立 `client_secret` 能力不变。

## 本次是否涉及配置变更
- 否

## 本次是否涉及数据结构/迁移
- 否
- 延续上一版已执行的 `0012_accounts_client_secret.sql`。

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的标准流程执行。
- 发布来源为 `dev`。
- 先 push 提交与 tag，等待 GHCR 固定 tag 镜像可用。
- 服务器侧仅执行固定 tag 镜像拉取、容器更新与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 线上实测商家四段格式账号取件
- 线上实测系统原生 OAuth 账号取件

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 切回上一个已验证 tag：`2026.05.10-5`。

## 风险说明
- 本次只修正微软取件调用链的参数传递，风险集中在微软邮箱取件路径，需要继续关注 Graph / IMAP 双通道回退结果。

## 审核状态
- 审核通过：用户已明确要求通了就直接部署服务器。

## 部署状态
- 未部署
