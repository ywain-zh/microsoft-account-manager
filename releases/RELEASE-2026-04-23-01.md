# RELEASE-2026-04-23-01

## 本次发布日期
- 2026-04-23

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.23-1`

## 本次发布目的
- 修复 779 新接口验卡时误带当前账号 inviterCode 导致返回错误业务信息的问题。
- 让已过期卡密能够正确识别并返回“卡密已过期”。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `server/app.ts`
- `releases/RELEASE-2026-04-23-01.md`

## 本次更新内容摘要
- 779 open-api 登录成功后不再默认回填当前账号自己的 `inviteCode` 作为 `inviterCode`。
- 兑换请求仅在显式配置 `SEVEN79_OPEN_API_INVITER_CODE` 时才携带 `inviterCode`。
- 修复部分已过期卡密被上游错误返回 `cannot use your own invite code` 的问题。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不涉及数据库结构变更，仅修正 779 新接口请求参数逻辑。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.23-1`、确认服务器可拉取本次镜像，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 若镜像短暂未可拉取，必须继续自动重试，镜像可拉取后直接继续部署，不得中途停下。

## 本次验证范围
- `vue-tsc --noEmit`
- `tsc -p tsconfig.server.json --noEmit`
- 确认 779 过期卡密不再返回 `cannot use your own invite code`。
- 确认过期卡密错误能归一化为中文 `卡密已过期`。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-8`。
- 本次不涉及数据库结构变更，无需额外执行迁移回滚。

## 风险说明
- 本次仅调整 779 新接口请求参数构造，部署后应重点回归验卡失败场景、过期卡密提示文案以及正常提取链路。
- 如果后续需要显式绑定 inviterCode，仍需通过环境变量配置，不可再默认复用登录账号自身的邀请码。

## 审核状态
- 审核通过

## 部署状态
- 已部署
