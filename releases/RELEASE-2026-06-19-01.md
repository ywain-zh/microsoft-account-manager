# RELEASE 2026-06-19-01

## 本次发布日期
- 2026-06-19

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.19-1`

## 本次发布目的
- 发布公益站签到能力与系统代理配置。
- 移除长链生成器及其后端、运行时、Docker 专用依赖。
- 部署最新望月工具箱到阿里云服务器。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `src/views/PublicCheckinView.vue`
- `src/state/public-checkin-console.ts`
- `server/runtime/public-checkin.ts`
- `server/main.ts`
- `server/runtime/env.ts`
- `src/views/SystemSettingsView.vue`
- `src/api.ts`
- `src/types.ts`
- `src/config/navigation.ts`
- `src/router.ts`
- `migrations/0017_public_checkin.sql`
- `migrations/0018_remove_long_link_settings.sql`
- `Dockerfile`
- `package.json`
- `DEPLOY_LITE.md`
- `README.md`
- 删除长链生成器相关页面、后端 runtime、Python helper、测试和 runtime 复制脚本。

## 本次更新内容摘要
- 新增公益站签到页面与后端能力，支持账号管理、站点管理、凭证加密、连接检测、手动/定时签到、余额刷新、日志、统计、代理和防护页处理。
- 系统设置新增本地代理配置，公益站账号可选择使用服务器配置的代理流量。
- 公益站签到 UI 调整为当前管理台样式，并按参考设计稿重排列表、工具栏和弹窗。
- 完整移除长链生成器：导航、路由、前端页面、前端 API/types、后端 API、运行时 helper、测试、Docker Python venv 与 `curl_cffi`。
- 新增迁移清理旧 `sub2api_long_link_config` 配置。
- 部署文档补充旧镜像清理策略，避免小规格服务器磁盘被历史镜像占满。

## 本次是否涉及配置变更
- 是
- 部署时需将服务器 `.env` 中 `APP_IMAGE` 更新为 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.19-1`。
- 系统代理可在后台“系统设置 -> 代理”中配置，无需新增环境变量。

## 本次是否涉及数据结构/迁移
- 是
- `0017_public_checkin.sql` 新增公益站签到相关表。
- `0018_remove_long_link_settings.sql` 删除旧长链生成器配置残留。
- 迁移为前向变更；回滚镜像前如需严格回滚数据，请先恢复部署前 SQLite 备份。

## 本次部署步骤
- 严格按 `DEPLOY_LITE.md` 的后续升级部署步骤执行。
- 服务器只执行固定 tag 镜像 `pull` / `up -d` / 健康检查 / 旧镜像清理，不在服务器构建。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- `node --experimental-strip-types --test tests/public-checkin.test.ts`
- 长链残留扫描：代码、构建、测试目录仅保留迁移删除语句中的旧 key。

## 本次回滚方式
- 上一版已验证 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.05-2`
- 回滚时将服务器 `.env` 的 `APP_IMAGE` 切回上一版 tag，执行 `docker compose pull && docker compose up -d`。
- 如新迁移数据需要回滚，恢复部署前备份的 `data/account-manager.db`。

## 风险说明
- 本次包含数据库迁移和新定时任务，部署后需重点观察容器健康、日志、SQLite 数据目录、公益站签到调度器启动情况。
- 长链生成器已完整删除，线上将不再提供对应页面和 API。

## 审核状态
- 审核通过

## 部署状态
- 已部署
