# Release 2026.05.12-2

## 本次发布日期
- 2026-05-12

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.05.12-2`

## 本次发布目的
- 修复 Cloud Mail 收件箱左侧邮件列表摘要直接显示 HTML 源码的问题。
- 与微软邮箱收件箱的预览处理保持一致，列表摘要展示可读正文文本。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/app.ts`
- `src/utils/mail-preview.ts`
- `releases/RELEASE-2026-05-12-02.md`

## 本次更新内容摘要
- Cloud Mail 后端生成邮件 `preview` 时，会从 HTML 或转义后的 HTML 中提取纯文本摘要。
- Cloud Mail 后端 HTML 类型判断收窄到真实邮件标签，避免邮箱地址中的尖括号被误判。
- 前端收件箱摘要渲染增加兜底清洗：旧缓存中的 HTML/转义 HTML 摘要也会先转为纯文本再展示。
- 邮件正文 HTML 渲染逻辑保持不变，仅修复列表预览摘要。

## 本次是否涉及配置变更
- 否
- 不新增环境变量，不修改部署参数。

## 本次是否涉及数据结构/迁移
- 否
- 不涉及数据库结构变更。

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的标准流程执行。
- 发布来源为 `dev`。
- 先 push 提交与 tag，等待 GHCR 固定 tag 镜像可用。
- 服务器侧仅执行固定 tag 镜像拉取、容器更新与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build:web`
- `npm run build:server`
- 本地确认 Cloud Mail 摘要处理逻辑会将 HTML/转义 HTML 转为纯文本。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 切回上一个已验证 tag：`2026.05.12-1`。
- 本次不涉及数据库迁移，无需数据库结构回滚。

## 风险说明
- 本次调整 Cloud Mail 邮件摘要生成逻辑，若远端返回特殊格式内容，摘要可能比原先更短，但正文渲染不受影响。
- 前端兜底清洗会影响所有收件箱列表摘要，但只处理疑似 HTML 的内容，普通邮箱地址与文本不会被剥离。

## 审核状态
- 审核通过：用户已明确要求直接部署到阿里云服务器，不再等待后续指令。

## 部署状态
- 待部署
