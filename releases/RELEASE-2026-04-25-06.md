# RELEASE 2026-04-25-06

## 本次发布日期
- 2026-04-25

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-6`

## 本次发布目的
- 修复 Stripe 支付脚本运行镜像缺少 Python 运行环境和 `pay.py` 文件导致执行失败的问题。

## 本次来源分支
- `dev`
- 本次发布直接从 `dev` 发出，不涉及临时发布分支。

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `Dockerfile`
- `server/app.ts`
- `releases/RELEASE-2026-04-25-06.md`

## 本次更新内容摘要
- 运行镜像安装 `python3` 和 `py3-requests`，满足 `pay.py` 的运行依赖。
- 将构建阶段的 `pay.py` 复制到运行镜像 `/app/pay.py`。
- 后端 Stripe 支付执行器默认 Python 命令由 `python` 改为 `python3`，匹配 Alpine 镜像环境。

## 本次是否涉及配置变更
- 否
- 继续使用 `PAY_CONFIG_DEFAULT_PATH=/app/data/config.json` 读取服务器外置配置。
- 仅需部署固定镜像 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-6`。

## 本次是否涉及数据结构/迁移
- 否

## 本次部署步骤
- 严格按照 `DEPLOY_LITE.md` 的后续升级部署步骤执行。
- 发布来源必须是 `dev`。
- 服务器只执行固定 tag 镜像 pull、compose up、状态检查、日志检查、资源检查和健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 部署后确认容器内存在 `python3`、`/app/pay.py`，并确认 `/api/health` 正常。

## 本次回滚方式
- 如部署后异常，将 `APP_IMAGE` 切回上一版已验证固定 tag：`ghcr.io/ywain-zh/microsoft-account-manager:2026.04.25-5` 后执行 `docker compose pull` 和 `docker compose up -d`。
- 本次不涉及数据库迁移，不需要恢复数据库备份。

## 风险说明
- 镜像会增加 Python/requests 运行依赖，镜像体积会增加。
- 支付脚本仍依赖服务器 `/app/data/config.json` 中的敏感配置，应继续保持文件权限受限。

## 审核状态
- 用户运行 Stripe 支付脚本时报错，已确认根因为运行镜像缺少 Python 与 `pay.py`。

## 部署状态
- 已部署
