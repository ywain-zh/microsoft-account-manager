# RELEASE-2026-04-22-06

## 本次发布日期
- 2026-04-22

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-6`

## 本次发布目的
- 将 PP 接码手机号展示改成单个下拉框，减少右侧信息重复占位。
- 调整 PP 接码号码和验证码展示方式，使号码选择与复制逻辑更符合使用习惯。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/views/Seven79CardsView.vue`
- `releases/RELEASE-2026-04-22-06.md`

## 本次更新内容摘要
- PP 接码区取消上方独立号码行，改成单个下拉框直接选择手机号。
- 下拉项中的手机号改成“(+区号) 号码 · 状态”格式，例如 `(+1) 5722318615 · 可用`。
- PP 接码复制按钮改为仅复制号码主体，不再复制前缀区号。
- PP 验证码展示区域改为卡片内居中显示，避免视觉偏左。

## 本次是否涉及配置变更
- 否
- 本次无需新增服务器环境变量；部署时仅需将 `APP_IMAGE` 更新到本次固定 tag。

## 本次是否涉及数据结构/迁移
- 否
- 本次不新增数据库迁移，仅调整 779 前端展示样式与交互。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先完成本地验证、推送 `dev`、推送固定 tag `2026.04.22-6`、确认 GHCR 镜像可用，再在服务器执行文档中的检查、`.env` 备份、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。
- 若镜像暂未可用，必须继续等待并自动重试，镜像可用后直接继续部署，不得中途停下。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 确认 PP 接码区仅显示一个手机号下拉框。
- 确认下拉项区号以前缀括号形式显示，复制按钮仅复制号码主体。
- 确认 PP 验证码在卡片内居中显示。

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.22-5`。
- 本次不涉及数据库结构变更，无需额外执行迁移回滚。

## 风险说明
- 本次主要为 PP 接码区展示与交互调整，部署后应重点回归手机号切换、复制结果和验证码可读性。
- 如果后续接入非 +1 长度号码，需要继续关注前缀与主体的拆分展示是否符合预期。

## 审核状态
- 审核通过

## 部署状态
- 未部署
