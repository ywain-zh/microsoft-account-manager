# RELEASE-2026-04-18-01

## 本次发布日期
- 2026-04-18

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-1`

## 本次发布目的
- 重构 Sub2API 检测页的视觉与布局，将连接配置改为弹窗录入，并提升统计区与日志区的密度和可读性。
- 回退仓库分支策略，恢复以后直接在 `dev` 分支开发，并同步更新相关文档与发布约束。

## 本次来源分支
- `dev`

## 本次集成分支
- 固定填写：`dev`

## 本次是否涉及上游同步
- 否
- 本次不包含 `upstream/main -> main -> dev` 的同步内容。

## 本次是否涉及冲突处理
- 否
- 本次未发生需要记录的 Git 冲突处理。

## 本次修改文件列表
- `src/views/Sub2ApiCheckerView.vue`
- `README.md`
- `BRANCH_LAYOUT.md`
- `DEPLOY_LITE.md`
- `releases/RELEASE_TEMPLATE.md`
- `CLAUDE.md`
- `releases/RELEASE-2026-04-18-01.md`

## 本次更新内容摘要
- 将 Sub2API 检测页的连接配置区抽离到 `n-modal`，保留原有 `v-model`、按钮事件和检测逻辑。
- 将统计区重构为单行 8 列高密度数据卡片，并将日志区重构为深色终端风格。
- 为日志等级补充视图层色标映射：`warning/error -> WARN`、`info -> INFO`、`success -> OK`。
- 将项目工作流文档回退为直接在 `dev` 分支开发，并移除 `clouded` / `codex` 的活动工作流说明。

## 本次是否涉及配置变更
- 否
- 本次不新增环境变量，不修改服务器 `.env` 结构。

## 本次是否涉及数据结构/迁移
- 否
- 本次不涉及数据库结构调整和迁移脚本。

## 本次部署步骤
- 必须严格按 `DEPLOY_LITE.md` 执行，不允许偏离文档流程。
- 发布来源固定为 `dev`。
- 先推送 `dev`，再推送固定 tag `2026.04.18-1`，等待 GHCR 镜像可用。
- 服务器侧只允许执行文档中的检查命令、`docker compose pull`、`docker compose up -d`、日志检查、资源检查与健康检查。

## 本次验证范围
- `npm run typecheck`
- `npm run build`
- 复核 Sub2API 检测页的配置弹窗、统计区、按钮工具栏和日志终端绑定未被破坏

## 本次回滚方式
- 如需回滚，按 `DEPLOY_LITE.md` 的回滚标准命令，将 `APP_IMAGE` 切回上一个已验证版本 `ghcr.io/ywain-zh/microsoft-account-manager:2026.04.17-3`。
- 本次不涉及数据库迁移，如仅回滚本次页面与文档调整，无需恢复 SQLite 备份。

## 风险说明
- 本次主要风险在于 Sub2API 检测页视图重构可能影响按钮可用性、弹窗配置保存体验和日志展示可读性。
- 服务器部署前必须确认 GHCR 已成功生成固定 tag `2026.04.18-1` 的镜像，否则必须停止，不允许继续升级。

## 审核状态
- 审核通过

## 部署状态
- 已部署

## 部署记录
- 部署时间：2026-04-18
- 部署方式：严格按 `DEPLOY_LITE.md` 的升级部署命令执行
- 服务器：`root@47.251.82.144:/opt/microsoft-account-manager`
- 部署结果：`.env` 已切换到 `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.04.18-1`
- 容器结果：`microsoft-account-manager` 已重建并启动，状态为 `running`，随后健康检查通过
- 备份文件：`.env.bak.2026.04.18-1`

## 部署后验证结果
- 本地验证通过：`npm run typecheck`、`npm run build`
- 服务器容器状态：`docker compose ps` 显示 `Up ... (health: starting)`，随后健康接口返回正常
- 健康检查：`docker compose exec -T microsoft-account-manager wget -qO- http://127.0.0.1:8787/api/health` 返回 `{"ok":true}`
