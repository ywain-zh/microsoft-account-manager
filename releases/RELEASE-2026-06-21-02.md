# RELEASE 2026-06-21-02

## 本次发布日期
- 2026-06-21

## 本次发布版本/tag
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-2`

## 本次发布目的
- 修复公益站签到中 Any Router 账号保存模型检测 API Key 时报 `服务器内部错误` 的问题。

## 本次来源分支
- `dev`

## 本次集成分支
- `dev`

## 本次是否涉及上游同步
- 否

## 本次是否涉及冲突处理
- 否

## 本次修改文件列表
- `server/runtime/public-checkin.ts`
- `tests/public-checkin.test.ts`

## 本次更新内容摘要
- Any Router 仍作为运行时适配器识别，用于签到、余额、检测和模型列表请求。
- 站点写入数据库前将 `anyrouter` 映射为兼容旧表约束的 `new-api`，避免触发 SQLite CHECK 约束错误。
- 账号运行时创建适配器时补充站点名称上下文，确保仅通过站点名识别 Any Router 时仍走专用适配器。
- 补充回归测试，覆盖 Any Router 存储平台映射和运行时适配器选择。

## 本次是否涉及配置变更
- 否。

## 本次是否涉及数据结构/迁移
- 否。
- 本次不新增迁移、不改线上 SQLite 表结构；旧表约束保持不变。

## 本次部署步骤
- 必须严格执行 `DEPLOY_LITE.md` 的后续升级部署步骤。
- 发布来源固定为 `dev`。
- 服务器只允许拉取固定镜像 tag 并重启容器，不允许在服务器构建。

## 本次验证范围
- `node --test tests/public-checkin.test.ts`
- `npm run typecheck`
- `npm run build`

## 本次回滚方式
- 按 `DEPLOY_LITE.md` 回滚步骤切回上一版已验证镜像：
- `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-1`
- 本次不涉及数据库结构变更，回滚不需要恢复数据库结构。

## 风险说明
- 本次只改变 Any Router 平台的持久化值，不改变前端填写方式和 API Key 加密保存方式。
- 普通 `new-api`、`one-api`、`onehub` 站点保存行为保持不变。
- Any Router 运行时适配依旧依赖站点名或 URL 命中识别规则。

## 审核状态
- 用户已明确要求实施修复方案，视为审核通过并批准部署 hotfix。

## 部署状态
- 已部署

## 部署结果
- GitHub Actions `Publish GHCR Image` 已成功构建并推送镜像，运行 ID：`27893106194`。
- 服务器已同步 release 文档到 `/opt/microsoft-account-manager/releases/RELEASE-2026-06-21-02.md`。
- 服务器侧 `docker manifest inspect ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-2` 校验通过。
- 服务器 `/opt/microsoft-account-manager/.env` 已更新为 `APP_IMAGE=ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-2`。
- 已创建部署前备份：
  - `.env.bak.2026.06.21-2`
  - `data/backups/account-manager.2026.06.21-2.<timestamp>.db`
- 已执行 `docker compose pull` 和 `docker compose up -d`，未在服务器构建。
- 当前容器 `microsoft-account-manager` 使用镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-2`，状态为 `running healthy`。
- 容器内健康检查 `http://127.0.0.1:8787/api/health` 返回 `{"ok":true}`。
- 线上公网健康检查 `https://mail.0222999.xyz/api/health` 返回 HTTP 200 和 `{"ok":true}`。
- 线上首页 `https://mail.0222999.xyz/` 返回 HTTP 200，页面标题为 `望月工具箱`。
- 容器资源占用正常，部署后内存约 `73.98MiB / 512MiB`。
- 已清理旧项目镜像 `ghcr.io/ywain-zh/microsoft-account-manager:2026.06.20-1`。
- 当前保留镜像：
  - 当前版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-2`
  - 回滚版本：`ghcr.io/ywain-zh/microsoft-account-manager:2026.06.21-1`
- 清理前根分区约 `29%` 使用率，清理后约 `28%` 使用率。
- 未执行回滚。
