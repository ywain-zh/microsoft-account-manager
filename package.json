{
  "name": "cf-account-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:web": "vite",
    "dev:server": "node --watch --experimental-strip-types server/main.ts",
    "build:web": "vite build",
    "build:server": "esbuild server/main.ts server/cli/migrate.ts server/cli/import-legacy.ts --bundle --platform=node --format=esm --target=node22 --external:better-sqlite3 --external:undici --external:imapflow --external:mailparser --external:nodemailer --outdir=build/server --outbase=server --entry-names=[dir]/[name]",
    "build": "npm run build:web && npm run build:server",
    "typecheck": "vue-tsc --noEmit && tsc -p tsconfig.server.json --noEmit",
    "db:migrate": "node build/server/cli/migrate.js",
    "db:import-legacy": "node build/server/cli/import-legacy.js",
    "bug team授权登录": "node \"scripts/bug team授权登录.mjs\" --automate",
    "start": "node build/server/main.js"
  },
  "engines": {
    "node": ">=22.22.0"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "dompurify": "^3.4.0",
    "hono": "^4.8.2",
    "imapflow": "^1.4.2",
    "mailparser": "^3.9.12",
    "naive-ui": "^2.40.3",
    "nodemailer": "^9.0.1",
    "playwright": "^1.60.0",
    "undici": "^8.5.0",
    "vue": "^3.5.13",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@hono/node-server": "^1.14.4",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.15.14",
    "@vitejs/plugin-vue": "^5.2.4",
    "esbuild": "^0.25.3",
    "typescript": "^5.8.3",
    "vite": "^5.4.10",
    "vue-tsc": "^2.2.8"
  }
}
